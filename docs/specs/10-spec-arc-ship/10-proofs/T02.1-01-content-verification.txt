PROOF: Content Verification
Type: file
File: skills/arc-wave/SKILL.md
Section: Step 7: Update BACKLOG
Command: grep -A 10 "### Step 7: Update BACKLOG" skills/arc-wave/SKILL.md
Timestamp: 2026-04-13T00:00:00Z

Expected:
- Spec field added after Wave line in template
- Documentation note about /cw-spec updating the Spec field
- All existing Step 7 content intact

Result:
### Step 7: Update BACKLOG

For each selected idea in `docs/BACKLOG.md`:

1. **Update summary table row:** Change status to `spec-ready`, set wave column to wave name
2. **Update idea section:** Add wave assignment and spec lines:
   ```markdown
   - **Wave:** Wave {N} — {Wave Name}
   - **Spec:** (set during /cw-spec)
   ```
   > The `/cw-spec` command will update the **Spec:** field with the actual spec directory path (e.g., `docs/specs/04-spec-core-capture`) when creating the spec.
3. Change status from `shaped` to `spec-ready`

Status: PASS
Verification: Both the Spec field and the documentation note are correctly added. The template now includes:
1. The Spec field placeholder with "(set during /cw-spec)"
2. A blockquote note explaining that /cw-spec will update this field
3. All existing content remains intact (step 1 and 3 unchanged)
