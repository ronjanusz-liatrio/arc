# T01.1 Proof Summary

**Task:** T01.1 — Create SKILL.md skeleton with frontmatter, overview, walkthrough, and constraints
**Spec:** 10-spec-arc-ship / Unit 1: Core Ship Skill — Single Idea Flow
**Completed:** 2026-04-13T00:00:00Z

## Artifacts

| File | Type | Status |
|------|------|--------|
| T01.1-01-file.txt | file | PASS |
| T01.1-02-cli.txt | cli | PASS |

## Summary

Created `skills/arc-ship/SKILL.md` with:

- Frontmatter matching arc-capture pattern: `name: arc-ship`, `description`, `user-invocable: true`, `allowed-tools: Glob, Grep, Read, Write, Edit, AskUserQuestion`
- `# /arc-ship` heading and context marker instruction
- `## Overview` section describing the skill's purpose
- `## Walkthrough` with mermaid flowchart using Arc brand theme (`#11B5A4` teal, `#E8662F` orange, `#1B2A3D` navy), 14 nodes (≤15 limit), depicting: Start → Read BACKLOG → Select idea → Resolve spec path → Spec field check → Find validation report → Ask user for path → Overall PASS check → Report failure → Update BACKLOG → Update ROADMAP → Refresh CLAUDE.md → Confirm → End
- `## Critical Constraints` section with 9 constraints covering verification requirements, read-only rules, and always-begin marker
- `## Process` section with 8 steps covering the full single-idea ship flow

`bash scripts/lint-mermaid.sh` passes with 13 files, 15 fences (new diagram validated as 1 of those fences).
