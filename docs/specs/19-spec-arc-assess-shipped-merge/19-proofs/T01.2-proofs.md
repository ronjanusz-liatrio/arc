# T01.2 Proof Summary

**Task:** T01.2 — Branch KW-19 classification with merge-candidate routing in SKILL.md
**Spec:** 19-spec-arc-assess-shipped-merge
**Native task ID:** 16
**Status:** COMPLETED
**Timestamp:** 2026-05-11T10:38:00Z

## Scope

Updated `skills/arc-assess/SKILL.md` Step 2d (Classify discoveries) to branch
KW-19 (`## User Stories`) classification on the shipped-spec index built by
Step 1.6. Two edits inside the spec-specific-classification block:

1. **KW-19 row in the spec-specific classification table** (line 528) — replaced
   the "BACKLOG" target with "BACKLOG (or merge-candidate routing for shipped-spec
   sources)" and appended a cross-reference to the new sub-rule below.

2. **New "KW-19 Shipped-Spec Merge-Candidate Branch" sub-rule** (lines 534-582)
   inserted between the spec-specific classification table and decision rule 3
   ("Merged-section handling"). The sub-rule documents:
   - Lookup-result routing table (absent → standard captured-stub; single
     match → auto-route; ≥2 matches → AskUserQuestion).
   - Multi-match `AskUserQuestion` prompt with options keyed by spec-dir-basename
     (label) and wave archive file + skill heading (description), plus
     `Skip this source` as the last option.
   - User-selection handling: a selected candidate writes a merge-candidate
     row with unselected candidates rendered as nested sub-bullets; `Skip this
     source` writes the row with a `(skipped by user)` annotation and does
     not auto-route.
   - Suppressions (no captured stub, no align-manifest.md row).
   - Preserved sub-steps (persona extraction continues on shipped-spec sources).
   - No-op when index is empty.
   - Cross-reference to `import-rules.md` for canonical routing rules.

KW-1..KW-18 and KW-20..KW-22 classification prose is byte-identical to the
parent commit. The only deletion in the diff is the original KW-19 row, which
the task explicitly authorizes for update.

## Files Modified

- `skills/arc-assess/SKILL.md` — 51 insertions, 1 deletion (KW-19 row replaced
  inline + new sub-rule appended below the spec-specific classification table).

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | file | `T01.2-01-file.txt` | PASS |
| 2 | test | `T01.2-02-test.txt` | PASS |
| 3 | test | `T01.2-03-test.txt` | PASS |

## Verification Highlights

- `git diff --numstat HEAD skills/arc-assess/SKILL.md` → `51 1` — single
  deletion is the original KW-19 row (allowed by task scope).
- `git diff | grep -E "^-[^-]"` returns exactly one line: the original KW-19
  row replacement. No KW-1..KW-18 or KW-20..KW-22 prose was deleted.
- Anchor strings present:
  * "merge-candidate routing for shipped-spec sources" — line 528 (table row)
  * "KW-19 Shipped-Spec Merge-Candidate Branch:" — line 534 (sub-rule heading)
  * "Auto-route to that target without prompting" — line 541 (single-match)
  * "AskUserQuestion" — line 549 (multi-match prompt)
  * "Skip this source" — lines 557, 564, 569
  * "Persona extraction" — line 578
  * "No `align-manifest.md` row" — line 574
- All five spec functional requirements (FR for single-match auto-route,
  multi-match prompt, Skip this source, persona-extraction preserved, no
  align-manifest row) are documented in the new sub-rule.
- Cross-reference to `import-rules.md` "Shipped-Spec Merge Candidates: KW-19
  Routing Override" section is present at line 582 (T01.3 sibling target).
- Cross-reference to Step 1.6 (built by T01.1) is present at line 536 and
  line 580.

## Spec Invariant Check

> "Keep KW-1..KW-18 and KW-20..KW-22 classification logic byte-for-byte
> unchanged in skills/arc-assess/SKILL.md and the references."
> — 19-spec-arc-assess-shipped-merge.md, Functional Requirements

Confirmed via:
- `git diff` showing zero `-` lines containing any KW-N for N in
  {1..18, 20..22}.
- The only `-` line in the SKILL.md diff is the original KW-19 row, which
  is explicitly authorized by the task ("Update skills/arc-assess/SKILL.md
  KW-19 classification prose to branch on the Shipped-Spec Index").

## Sanitization

Scanned all `T01.2-*` proof artifacts for credentials, tokens, API keys,
session strings, and private keys. None found. All proof file contents are
derived from `git diff` output and `grep` line numbers — no sensitive data.

Status: CLEAN
