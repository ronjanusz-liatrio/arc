# T01.5 Proof Summary

**Task:** Extend Lifecycle Gaps emit logic with LG-6 row format

**Spec:** docs/specs/18-spec-arc-status-spec-completion (Unit 1, FR7)

**Worker:** worker-1

**Status:** COMPLETED

## Scope

Extend the **Emit Lifecycle Gaps** subsection (lines 271-318) of `skills/arc-status/SKILL.md` to render LG-6 (Orphan Spec) rows in both rendering branches:

1. **Three-column (no-wave) branch** — append a new row immediately after the `Validation -> Shipped` row:
   `| Orphan Spec | {NN}-spec-{name} | Run /arc-capture |`
2. **Four-column (active-wave) branch** — append a new row immediately after the `Validation -> Shipped` row:
   `| Orphan Spec | {NN}-spec-{name} | Run /arc-capture | {Scope Cell} |`
3. **Document the always-`Backlog (outside wave)` Scope cell rule for LG-6** — add a new bullet point in the four-column branch's `{Scope Cell}` derivation list, between the existing `backlog-only` bullet and the `skipped-check` bullet, explaining that LG-6 always renders `Backlog (outside wave)` because Step 6.6 rule 6 unconditionally tags LG-6 gaps as `scope = backlog-only`.

This connects T01.1 (LG-6 detection), T01.2 (Step 6.6 LG-6 -> backlog-only rule), and T01.3 / T01.4 (Step 7 priorities) to the user-visible output: a detected LG-6 gap now produces a row in the rendered Lifecycle Gaps table.

## Co-Edit Coordination

worker-2 was running T02.3 (verification only, read-only) concurrently. No file conflicts because:

- T02.3 is read-only (verification of Step 4 In-Flight Specs filter logic).
- T01.5's edits are confined to the `#### Emit Lifecycle Gaps` block (Step 6 emit logic), which Step 4 does not touch.

The git working tree at the time of edit had unrelated modifications and untracked files (BACKLOG.md, ROADMAP.md, shape-report.md, wave-report.md, and spec 18 source artifacts). These are NOT part of T01.5's scope. The commit stages only `skills/arc-status/SKILL.md` and the T01.5 proof artifacts under `docs/specs/18-spec-arc-status-spec-completion/18-proofs/`.

## Proof Artifacts

| # | File | Type | Status | Description |
|---|------|------|--------|-------------|
| 1 | `T01.5-01-file.txt` | file | PASS | Shows the full post-edit `#### Emit Lifecycle Gaps` section (lines 271-318) with both new "Orphan Spec" rows visible and the new LG-6 Scope-cell bullet. |
| 2 | `T01.5-02-test.txt` | test | PASS | Byte-for-byte equality check of LG-1..LG-5 rows in both tables between HEAD and working tree -- empty diff confirms zero changes. |
| 3 | `T01.5-03-test.txt` | test | PASS | Byte-for-byte equality check of the no-gaps message, the wave-name interpolation paragraph, and both skipped-check rendering rules between HEAD and working tree -- all empty diffs. |
| 4 | `T01.5-04-test.txt` | test | PASS | Full isolated `git diff HEAD` of `skills/arc-status/SKILL.md` showing exactly three additions and zero deletions. |
| 5 | `T01.5-05-test.txt` | test | PASS | Byte-for-byte equality check of Step 7's precedence + alt-skill tables, the Step 6.6 numbered list, and the LG-6 detection subsection between HEAD and working tree -- T01.5 did not touch tables/sections owned by T01.1, T01.2, T01.3, T01.4. |

## End-to-End LG-6 Pipeline Now Wired

After T01.5, the LG-6 (Orphan Spec) lifecycle gap is end-to-end wired:

| Step | Component | Owner | Behavior |
|------|-----------|-------|----------|
| 6.5 | LG-6 detection subsection | T01.1 | PASS-validated spec without BACKLOG idea -> flagged |
| 6.6 | Step 6.6 scope tagging | T01.2 | LG-6 gap -> `scope = backlog-only` (always) |
| 6.7 | Emit Lifecycle Gaps tables | **T01.5 (this task)** | LG-6 gap -> "Orphan Spec" row in both 3-col and 4-col branches; Scope cell = `Backlog (outside wave)` |
| 7 | Precedence list (no-wave) | T01.3 | LG-6 -> Priority 14 -> recommend `/arc-capture` |
| 7 | Alternative Skill Selection | T01.4 | LG-6 alternative -> `/arc-audit` (no lower-priority no-wave gap) |

The companion reference doc updates are owned by T01.6 (LG-6 detection prose in `status-dimensions.md`) and T01.7 (WL-3 LG-6 always-backlog-only rule). Together with T01.1 .. T01.7, this completes Unit 1 (LG-6 Orphan Spec Detection) of spec 18. T01.8 verifies the full pipeline end-to-end against the live repo state.

## Out-of-Scope Items (Verified Untouched)

- LG-1..LG-5 detection subsections in Step 6 (T01.1 added LG-6 as a sibling without modifying earlier sections).
- Step 6.6 numbered list (T01.2 owns the LG-6 always-backlog-only rule there).
- LG-1..LG-5 rows in either Lifecycle Gaps table (verified byte-identical in T01.5-02).
- The `No lifecycle gaps detected.` message (verified byte-identical in T01.5-03).
- The wave-name-interpolation paragraph for `scope = wave-linked` (verified byte-identical in T01.5-03).
- Both skipped-check rendering rules (3-col and 4-col) (verified byte-identical in T01.5-03).
- Step 7 precedence + alt-skill tables (verified byte-identical in T01.5-05).
- Step 4 In-Flight Specs logic (T02.1 territory, untouched here).
