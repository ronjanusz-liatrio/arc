# T01.8 Proof Summary — Verify LG-6 invariants and idempotency end-to-end

**Task**: T01.8 (#10) — Final verification sub-task for T01 (Add LG-6 Orphan Spec Detection).
**Date**: 2026-05-08
**Worker**: worker-1
**Scope**: Two surgical narrative fixes in `skills/arc-status/SKILL.md` Step 7, plus comprehensive byte-for-byte invariant verification and end-to-end idempotency check.
**Parent commit (pre-T01)**: 995f82a `chore(arc): bump version to 0.21.0`

## Verification scope

This sub-task closes T01 by:
1. Applying the two stale-reference narrative fixes named in the task description (T01.3 and T01.4 left two now-stale Step 7 references behind; both surgical edits are in `skills/arc-status/SKILL.md`).
2. Proving the four byte-for-byte invariants the spec requires.
3. Confirming additive-only structural behaviour in the three regions where new content was inserted.
4. Running `/arc-status` (simulated deterministically per its SKILL.md prose) twice consecutively and proving byte-identical output (idempotency).

## Surgical narrative fixes (applied this sub-task)

| # | Location | Before | After | Artifact |
|---|----------|--------|-------|----------|
| 1 | Step 7 wave-state-aware paragraph (line 322 of `skills/arc-status/SKILL.md`) | "Priorities 9–14 apply when the active wave name is null" | "Priorities 9–15 apply when the active wave name is null" | `T01.8-05-file.txt` |
| 2 | Step 7 "Never offer the recommended skill as its own alternative" parenthetical (line 376) | "(e.g., both Priority 7 and Priority 14 recommend `/arc-wave`, but their alternatives differ)" | "(e.g., both Priority 7 and Priority 15 recommend `/arc-wave`, but their alternatives differ)" | `T01.8-05-file.txt` |

Both edits are pure narrative; no precedence logic, table content, or detection prose was modified.

A **third** stale "Priorities 9–14" cross-reference exists at line 265 inside Step 6.6 (postamble item 8 — "No active wave"). The T01.8 task description names only the two Step 7 references and explicitly forbids any other source-file edits in this sub-task. The line-265 reference is documented in `T01.8-05-file.txt` as a follow-up cleanup item.

## Invariants verified

| # | Invariant | Status | Artifact |
|---|-----------|--------|----------|
| 1 | LG-1..LG-5 detection prose in `skills/arc-status/SKILL.md` byte-for-byte unchanged vs. parent | PASS | `T01.8-01-test.txt` |
| 2 | Precedence-table rows 1–13 byte-for-byte unchanged vs. parent | PASS | `T01.8-02-test.txt` |
| 3 | Alternative-skill-table rows 1–13 byte-for-byte unchanged vs. parent | PASS | `T01.8-03-test.txt` |
| 4 | WL-3 LG-1..LG-5 entries in `references/status-dimensions.md` byte-for-byte unchanged vs. parent | PASS | `T01.8-04-test.txt` |

## Additive-only structural verification

| Region | Expected change | Actual change | Status |
|--------|-----------------|---------------|--------|
| Step 6.6 numbered list | exactly one insertion (new item 6 for LG-6) and exactly two renumbers (parent 6 → current 7, parent 7 → current 8) | confirmed (parent items 6 "Skipped checks" → current 7; parent item 7 "No active wave" → current 8; new item 6 "LG-6 (Orphan Spec)" inserted) | PASS |
| Precedence table | exactly one insertion (new Priority 14 for no-wave LG-6) and exactly one renumber (parent 14 → current 15) | confirmed (parent row 14 = current row 15 byte-equal after sed `14`→`15`; new row 14 = no-wave LG-6 → /arc-capture) | PASS |
| Alternative-skill table | corresponding insertion + renumber | confirmed (parent alt row 14 = current alt row 15 byte-equal after sed; new alt row 14 = `14 (no-wave LG-6) → /arc-audit`) | PASS |

## End-to-end /arc-status verification

Per task instructions: "run `/arc-status` against the current repo state and confirm: (a) `17-spec-claude-md-static-references` (and any other PASS-validated orphan spec) appears as an LG-6 row in the Lifecycle Gaps table; (b) running `/arc-status` twice consecutively produces identical inline output (idempotency)."

Because /arc-status is a slash-command-driven AskUserQuestion-bearing skill, the verification is conducted via a deterministic shell transcription of its SKILL.md prose (`run-arc-status-sim.sh`) that reads the same inputs and produces the same five summary sections plus the matched-priority recommendation. The simulator was executed twice back-to-back; both runs are captured and diffed.

| Check | Status | Artifact |
|-------|--------|----------|
| (a) `17-spec-claude-md-static-references` appears as an LG-6 row | PASS | `T01.8-06-cli-run1.txt`, `T01.8-08-cli-idempotency.txt` |
| (a, additional) Other PASS-validated orphan specs (03, 10, 11, 12, 13, 14, 16) also appear as LG-6 rows | PASS | `T01.8-06-cli-run1.txt` |
| (b) Two consecutive runs produce byte-identical output | PASS | `T01.8-06-cli-run1.txt` vs `T01.8-07-cli-run2.txt` (diff empty) |

## Repository state

Per task instructions: "Beyond the two stale-reference fixes above, do NOT modify any other source files in this sub-task — verification only."

Modified source files (only the two surgical fixes):
- `skills/arc-status/SKILL.md` — lines 322 ("9–14"→"9–15") and 376 ("Priority 14"→"Priority 15"). No other lines touched. Verified by `git diff` showing exactly those two single-line edits.

No other source files modified. All other artefacts created by this task are proof files under `docs/specs/18-spec-arc-status-spec-completion/18-proofs/T01.8-*`.

Working-tree files outside the T01 verification scope (pre-existing, not modified by T01.8):
- `docs/BACKLOG.md`, `docs/ROADMAP.md`, `docs/skill/arc/shape-report.md`, `docs/skill/arc/wave-report.md` — left untouched; verified outside-scope per task constraints.

## Conclusion

All four byte-for-byte invariants PASS. All three additive-only structural checks PASS. End-to-end /arc-status simulation surfaces all PASS-validated orphan specs (including spec 17) as LG-6 rows. Two consecutive runs are byte-identical (idempotency PASS).

**T01.8 is complete; T01 is ready to be marked done by the dispatcher.**

A single follow-up cleanup is recommended (out of scope for T01.8): update the third stale "Priorities 9–14" reference at SKILL.md line 265 (Step 6.6 postamble) to "Priorities 9–15".
