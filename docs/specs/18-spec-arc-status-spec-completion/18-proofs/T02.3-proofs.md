# T02.3 Proof Summary — Verify In-Flight filter against repo state and detection-prose invariant

**Task**: T02.3 (#13) — Verification-only sub-task following T02.1 and T02.2.
**Date**: 2026-05-08
**Worker**: worker-2
**Scope**: Read-only verification — no source files modified.

## Verification scope

This sub-task validates five invariants for the In-Flight Specs filter (Step 4
of `skills/arc-status/SKILL.md`, SD-3 of
`skills/arc-status/references/status-dimensions.md`) against the live repo state.

| # | Invariant | Status | Artifact |
|---|-----------|--------|----------|
| 1 | Detection-prose byte-identical to pre-T02 parent (dc7cb75) | PASS | `T02.3-01-test.txt` |
| 2 | Shipped specs absent from In-Flight Specs table         | PASS | `T02.3-02-cli.txt` |
| 3 | Orphan specs (e.g. spec 17) still present                | PASS | `T02.3-03-cli.txt` |
| 4 | Empty-after-filter fallback (manual walkthrough)         | PASS | `T02.3-04-walkthrough.txt` |
| 5 | No regression on per-row Spec File / Plan / Validation cols | PASS | `T02.3-05-test.txt` |

## Findings

### 1. Detection-prose invariant (PASS)

The Step 4 per-row artifact detection prose — the three labelled paragraphs
`**a. Spec file:**`, `**b. Plan evidence:**`, `**c. Validation report:**` — is
byte-for-byte identical between the current working tree and commit
`dc7cb7507f72156f6d154d2a218cd0a18c774a5b` (T01.4, the immediate parent of the
T02.1 commit `bb9f6fa`). Both extracts are 523 bytes; `cmp` reports no
differences and `diff` exits 0.

### 2. Shipped specs absent (PASS)

The completed-spec set was recomputed from the live wave archives at run time
by grep'ing `docs/skill/arc/waves/*.md` for `### ` H3 titles and `**Spec:**`
fields:

```
01-spec-arc-plugin
02-spec-arc-plugin-enhancement
04-spec-arc-readme
05-spec-arc-help
06-spec-arc-align-enhance
07-spec-capture-speedup
10-spec-arc-ship
11-spec-shape-skill-discovery
12-spec-arc-status
13-spec-wave-archive
```

After applying the silent exclusion rule, all 6 of the canonical shipped specs
named in the task description are absent from the In-Flight Specs table:
`04-spec-arc-readme`, `05-spec-arc-help`, `10-spec-arc-ship`,
`11-spec-shape-skill-discovery`, `12-spec-arc-status`, `13-spec-wave-archive`.

### 3. Orphan specs still in-flight (PASS)

`17-spec-claude-md-static-references` has a PASS-validated report
(`17-validation-claude-md-static-references.md` contains `**Overall**: PASS`)
and is not referenced by any `Spec:` field in `docs/BACKLOG.md`. It is
**absent** from the wave archives, so it is **not** in the completed-spec set,
so it **remains** in the In-Flight Specs table — exactly as the spec requires
(orphans surface via LG-6, not via wave-archive exclusion).

### 4. Empty-after-filter fallback (PASS — manual walkthrough)

The live repo has 9 in-flight specs after exclusion, so the empty-after-filter
case is not reachable as a live test. A manual walkthrough was recorded
documenting:

- The post-T02 prose (Step 4 step 4) extends the trigger condition to include
  "every spec directory is in the completed-spec set".
- The fallback string `No specs found — run /cw-spec to create a specification.`
  is byte-identical to the pre-T02 empty-glob fallback.
- A synthetic dry-run shows the empty-after-filter path emits the same
  fallback notice.

### 5. No regression on per-row detection columns (PASS)

For all 9 NN prefixes rendered post-T02, the per-row Spec File / Plan /
Validation values were computed from the unchanged algorithm and recorded.
Because (a) the detection prose is byte-identical (invariant 1) and (b) T02.1
and T02.2 only edited prose, not validation reports, plan files, or commit
history, the pre-T02 and post-T02 yes/no values match by construction for
every commonly-rendered NN prefix.

## Repository state (read-only confirmation)

Per task instruction: "Do NOT modify any source files in this sub-task." No
files under `skills/`, `docs/specs/[!18]*`, `docs/BACKLOG.md`, or
`docs/skill/arc/` were modified. Only proof artifacts under
`docs/specs/18-spec-arc-status-spec-completion/18-proofs/T02.3-*` were
created.

## Conclusion

All 5 verification points PASS. No follow-up fix tasks required against
T02.1 or T02.2.
