# T02.1 Proof Artifacts

**Task:** T02.1 — Extend SKILL.md Step 4 with completed-spec set and exclusion rule
**Spec:** docs/specs/18-spec-arc-status-spec-completion/
**Worker:** worker-2
**Date:** 2026-05-08

## Summary

Extended `skills/arc-status/SKILL.md` Step 4 (In-Flight Specs) with two new ordered list items:
- New step 2 — computes the **completed-spec set** from `docs/skill/arc/waves/*.md` as the union of two signals: H3 subsection-title matches and `**Spec:** {path}` field matches (with whitespace trim, case-sensitive comparison, trailing-slash normalization on the Spec field, and final-path-component matching).
- New step 3 — **silently excludes** any spec directory whose basename is in the completed-spec set from the rendering pass; no count footer, no parenthetical, no separate "Completed Specs" table.

The previously-step-2 fallback notice was renumbered to step 4 and its prose was updated to clarify it covers BOTH the empty-glob case AND the empty-after-filter case (notice text "No specs found — run /cw-spec to create a specification." remains literally unchanged).

The previously-step-3 per-row artifact detection prose (Spec File / Plan / Validation) is renumbered to step 5 with **byte-identical** sub-bullet text. The previously-step-4 specs-table emit is renumbered to step 6 with byte-identical block content. The SD-3 cross-reference pointer is preserved and extended.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | file | `T02.1-01-file.txt` | PASS — Step 4 contents captured; six numbered items present; all required prose elements verified |
| 2 | test | `T02.1-02-test.txt` | PASS — Per-row artifact detection prose (sub-bullets a/b/c) is byte-identical to parent commit (md5 match: `91714aa83cb574dc044def6d55d0f48a`) |
| 3 | test | `T02.1-03-test.txt` | PASS — Specs-table emit block byte-identical to parent (md5 match: `a16c7cf4274dd907f73d183aa3bddf2b`); fallback notice text unchanged; SD-3 cross-reference present |
| 4 | test | `T02.1-04-test.txt` | PASS — Diff vs HEAD shows exactly 3 hunks, all within Step 4 line range (117-146); no incidental edits outside scope |

## Acceptance Criteria Coverage

| Spec FR / Gherkin scenario | Proof |
|---------------------------|-------|
| FR: New step 2 defines completed-spec set as union of subsection-title and Spec-field signals | Proof 1 (item 2 prose verbatim) |
| FR: New step 3 silently excludes (no count footer, no parenthetical, no second table) | Proof 1 ("Exclusion is **silent** — emit no count footer, no parenthetical, and no separate \"Completed Specs\" table.") |
| Gherkin: "Filter is silent — no count footer or 'Completed Specs' sidebar is emitted" | Proof 1 (item 3 prose); Proof 3 (no extra table block in diff) |
| Gherkin: "Empty-after-filter case emits the existing fallback notice" | Proof 1 (item 4 prose covers both cases; literal notice text preserved); Proof 3 (single fallback occurrence) |
| Gherkin: "Step 4 per-row artifact detection prose remains byte-identical after the filter is added" | Proof 2 (md5 match across sub-bullets a/b/c) |
| Gherkin: "Per-row artifact detection columns are unchanged for in-flight rows" | Proof 2 (sub-bullet prose unchanged means downstream column rendering unchanged) |
| Gherkin: "In-flight specs are rendered sorted ascending by NN prefix after exclusion" | Proof 1 (item 5 retains "sorted by NN prefix ascending" verbatim) |
| Gherkin: "Trailing slash on a wave-archive Spec field is normalized before comparison" | Proof 1 (item 2b: "after whitespace trim and stripping a single trailing slash") |
| Gherkin: "Substring-but-not-exact title in a wave archive does not exclude a spec" | Proof 1 (item 2a: "exactly matches a spec directory basename"; item 3 reinforces basename-set membership) |
| FR: SD-3 cross-reference pointer present | Proof 1 / Proof 3 (line 149: "See `skills/arc-status/references/status-dimensions.md` (SD-3)...") |
| Constraint: Do NOT touch per-row artifact detection prose (current step 3, lines 126-132) | Proof 2 (byte-identical md5) |
| Constraint: Do NOT touch table emit format (current step 4) | Proof 3 (byte-identical md5 of table block) |
| Constraint: Do NOT modify NN-prefix-ascending sort | Proof 1 (item 5 retains sort directive verbatim) |
| Constraint: Do NOT touch any other Step | Proof 4 (3 hunks, all within Step 4 line range) |

## Notes on Co-Edit

Worker-1 was concurrently editing the same file (skills/arc-status/SKILL.md) for T01.4, working in Step 7 around lines 360+. T01.4 committed as dc7cb75 between my Read and final Edit. My Step 4 anchors held — the Edit applied cleanly. My byte-identity checks were re-verified against the new HEAD (dc7cb75) and remained PASS.

## Status

**ALL PROOFS PASS.** Implementation complete; ready for commit.
