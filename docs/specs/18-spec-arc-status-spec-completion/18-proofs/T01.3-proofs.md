# T01.3 Proof Summary

**Task:** Insert no-wave LG-6 row into Step 7 precedence table (renumber 14→15)
**Spec:** `docs/specs/18-spec-arc-status-spec-completion/18-spec-arc-status-spec-completion.md` (Unit 1, FR5 + FR9)
**File modified:** `skills/arc-status/SKILL.md`
**Status:** COMPLETED
**Timestamp:** 2026-05-08T00:00Z

## Scope

Pure-extension sub-task. Edited the `#### Precedence List` Markdown table inside `### Step 7: Next-Step Suggestion` of `skills/arc-status/SKILL.md`. Inserted a new Priority 14 row covering the no-wave LG-6 (Orphan Spec) case between the existing Priority 13 row (no-wave LG-1 P0/P1) and the previous Priority 14 row (no-wave, no gaps), and renumbered the previous Priority 14 row to Priority 15.

The new Priority 14 row reads:

> `| 14 | No active wave AND an LG-6 (Orphan Spec) gap exists | /arc-capture | "{NN}-spec-{name} has a PASS validation report but no BACKLOG idea — capture it?" |`

The previous Priority 14 row was renumbered to Priority 15 with no other text changes:

> `| 15 | No active wave AND no gaps | /arc-wave | "No gaps and no active wave — plan the next delivery wave?" |`

This is the only renumbering authorized by Unit 1 FR9 and applies only to the previous Priority 14 row in the Step 7 precedence table. The renumbering is confined to this single row — no other priority numbers, tables, or prose in the file are touched. Priorities 1–13 are byte-for-byte identical to HEAD.

## Out of Scope (Other Subtasks)

- T01.1 — Add LG-6 detection subsection to SKILL.md (between LG-5 and Step 6.6). (completed)
- T01.2 — Extend Step 6.6 numbered list with LG-6 always-backlog-only rule. (completed)
- T01.4 — Add no-wave LG-6 row to Alternative Skill Selection Rule table. (pending)
- T01.5 — Extend Lifecycle Gaps emit logic with LG-6 row format. (pending)
- T01.6 — Add LG-6 Orphan Spec subsection to status-dimensions.md. (completed)
- T01.7 — Extend WL-3 in status-dimensions.md with LG-6 always-backlog-only rule. (completed)
- T01.8 — Verify LG-6 invariants and idempotency end-to-end. (pending)

## Out of Scope (Other Units)

- Unit 2 — Filter In-Flight Specs by Wave Archive (T02.1 — T02.3).

## Out of Scope (Explicit Fences inside SKILL.md)

The T01.3 task description names four explicit out-of-scope regions inside the same file. All four are verified byte-identical to HEAD by `T01.3-03-test.txt`:

1. **Alternative Skill Selection table** — owned by T01.4. Still ends at Priority 14 ("no wave, no gaps"); T01.4 will renumber it.
2. **Wave-state-aware narrative paragraph above the precedence table** — still reads "Priorities 9–14 apply when the active wave name is null". Updating to "9–15" is not in T01.3's scope.
3. **P0/P1 filter, wave-scope, and wave-name-interpolation paragraphs below the precedence table** — unmodified.
4. **Priorities 1–13 in the precedence table** — byte-identical (separately verified by `T01.3-02-test.txt`).

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | file | `T01.3-01-file.txt` | PASS — Diff is purely additive (2 added / 1 removed); only the Priority 14 row changed (insertion + renumber-to-15). |
| 2 | test | `T01.3-02-test.txt` | PASS — Table header, separator, and Priority rows 1-13 are byte-identical to HEAD. |
| 3 | test | `T01.3-03-test.txt` | PASS — All four named scope fences (alternative-skill table, wave-state narrative, below-table paragraphs, Priorities 1-13) are byte-identical to HEAD. Whole-file diff is exactly one hunk at lines 332-333. |

## Spec Acceptance Criteria Coverage (Unit 1 — FR5 + FR9)

This sub-task satisfies the Step 7 precedence-table portion of Unit 1.

- [x] **New Priority 14 row inserted between current Priority 13 (no-wave LG-1 P0/P1) and current Priority 14 (no wave, no gaps).** — proof 1, line `+| 14 | No active wave AND an LG-6 (Orphan Spec) gap exists | /arc-capture | …`.
- [x] **Recommended skill = `/arc-capture`.** — proof 1, third pipe-cell of the new row.
- [x] **Reason template matches spec verbatim:** `"{NN}-spec-{name} has a PASS validation report but no BACKLOG idea — capture it?"`. — proof 1, fourth pipe-cell of the new row.
- [x] **Previous Priority 14 row ("No active wave AND no gaps") renumbered to Priority 15.** — proof 1, the line `+| 15 | No active wave AND no gaps | /arc-wave | …`. Body text of the row unchanged.
- [x] **No other priorities renumbered.** — proof 2 (Priorities 1-13 byte-identical) and proof 3 (whole-file diff has exactly one hunk).
- [x] **Priorities 1–13 byte-for-byte unchanged.** — proof 2 (empty diff against HEAD for lines 317-331).
- [x] **Alternative-skill table not touched (T01.4 owns it).** — proof 3, fence 1.
- [x] **Wave-state-aware narrative paragraph not touched.** — proof 3, fence 2.
- [x] **P0/P1 filter / wave-scope / wave-name-interpolation paragraphs not touched.** — proof 3, fence 3.

Functional requirements explicitly *not* covered by T01.3 (deferred to sibling sub-tasks):

- [ ] LG-6 detection subsection (T01.1, completed).
- [ ] Step 6.6 numbered-list LG-6 entry (T01.2, completed).
- [ ] Alternative Skill Selection Rule table no-wave LG-6 row + renumber (T01.4).
- [ ] Lifecycle Gaps emit logic LG-6 row format (T01.5).
- [ ] `status-dimensions.md` LG-6 detection prose (T01.6, completed).
- [ ] `status-dimensions.md` WL-3 extension (T01.7, completed).
- [ ] Wave-state narrative paragraph "Priorities 9–14" upper-bound update (deferred — not assigned to any sibling; will be caught by T01.8 verification or handled as a separate downstream edit).

## Editorial Decisions

- **Verbatim spec text.** The new Priority 14 row is copied verbatim from the T01.3 task description and matches Unit 1 FR5 exactly: condition text, recommended-skill cell `\`/arc-capture\``, and reason-template cell with the em-dash before "capture it?". The em-dash matches the U+2014 character used in sibling rows (Priorities 6, 13).
- **No body-text edits to the renumbered row.** Only the leading `| 14 |` cell of the previous Priority 14 row was changed to `| 15 |`. The condition, recommended-skill, and reason-template cells are byte-identical, preserving the row's existing wording for the empty-state case.
- **Wave-state narrative paragraph deliberately untouched.** The paragraph at line 313 still says "Priorities 9–14 apply when the active wave name is null". The T01.3 task description explicitly fences that paragraph as out-of-scope, so I left it unchanged even though it is now numerically stale by one. T01.8 (end-to-end verification) and the Unit 1 success criteria will determine whether that update is folded into a sibling task or handled as a separate post-merge edit.
- **Alternative-skill table deliberately untouched.** The table still has `| 14 (no wave, no gaps) | /arc-audit |` as its last row. T01.4 owns inserting a `| 14 (no-wave LG-6) | /arc-audit |` row and renumbering the previous Priority 14 row to Priority 15 in that table. The two tables intentionally fall out of sync between T01.3 and T01.4 — this is the agreed coordination shape per the task split.

## Sanitization

Proof artifacts contain only:

- Markdown content from the public spec (`18-spec-arc-status-spec-completion.md`).
- Prose excerpts and table rows from `skills/arc-status/SKILL.md` (a public reference doc).
- `git diff` and `diff` output (line numbers and quoted source lines from public files).

No secrets, tokens, credentials, connection strings, API keys, passwords, or sensitive data are present. Sanitization scan run: no hits for `sk-`, `pk_`, `api_key`, `apiKey`, `Bearer `, `password=`, `secret`, `token`, `credential`, `BEGIN PRIVATE KEY`, or SSH key markers in any T01.3-* artifact.
