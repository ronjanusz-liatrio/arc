# T01.4 Proof Summary

**Task:** Add no-wave LG-6 row to Alternative Skill Selection Rule table

**Spec:** docs/specs/18-spec-arc-status-spec-completion (Unit 1, FR6)

**Worker:** worker-1

**Status:** COMPLETED

## Scope

Single edit to the **Alternative Skill Selection Rule table** in Step 7 of `skills/arc-status/SKILL.md`:

1. **Insert** a new row at Priority 14 (no-wave LG-6) with alternative `/arc-audit (no lower-priority no-wave gap exists).`, mirroring the Priority 13 fall-through rule.
2. **Renumber** the previous Priority 14 (no wave, no gaps) row to Priority 15 — alternative skill (`/arc-audit`) and all other text byte-for-byte unchanged.

This keeps the Alternative Skill Selection Rule table in lockstep with the precedence table (T01.3) where the same Priority 14 → 15 renumbering occurred.

## Co-Edit Coordination

worker-2 was concurrently editing the same file (`skills/arc-status/SKILL.md`) for T02.1 in **Step 4** (lines ~117-150), while T01.4 edits **Step 7's alt-skill table** (lines ~349-365). The two edits are in disjoint sections.

When the working tree was prepared for commit, both T01.4 and T02.1 in-progress edits were present. To produce an atomic T01.4 commit (and avoid claiming worker-2's diff as part of T01.4), the T01.4 hunk was isolated:

1. `git diff HEAD -- skills/arc-status/SKILL.md` saved as backup patch (`/tmp/full_skill_diff.patch`).
2. `git checkout HEAD -- skills/arc-status/SKILL.md` restored the file to HEAD.
3. The T01.4-only hunk was extracted into `/tmp/my_skill_hunk.patch` and applied (`git apply --check` passed cleanly first).
4. Commit produced from this isolated state.
5. After commit, worker-2's hunks (`/tmp/worker2_hunks.patch`) were re-applied so the working tree state for T02.1 is preserved for worker-2 to commit independently.

This pattern keeps the T01.4 commit atomic and scoped to the alt-skill table while not disturbing worker-2's in-flight work.

## Proof Artifacts

| # | File | Type | Status | Description |
|---|------|------|--------|-------------|
| 1 | `T01.4-01-file.txt` | file | PASS | Shows the post-edit Alternative Skill Selection Rule table with the new Priority 14 (no-wave LG-6) row and renumbered Priority 15 (no wave, no gaps) row. |
| 2 | `T01.4-02-test.txt` | test | PASS | Byte-for-byte equality check of alt-skill table rows 1-13 between HEAD and working tree — zero changes confirmed. |
| 3 | `T01.4-03-test.txt` | test | PASS | Confirms the Step 7 precedence table (rows of the form `\| N \| ...`) is byte-identical between HEAD and working tree — T01.4 did not touch it (T01.3 owns it). |
| 4 | `T01.4-04-test.txt` | test | PASS | Full isolated `git diff HEAD` of `skills/arc-status/SKILL.md` showing the single hunk: removal of `\| 14 (no wave, no gaps) \| /arc-audit \|`, addition of the new Priority 14 (no-wave LG-6) row, and addition of the renumbered Priority 15 (no wave, no gaps) row. |

## Cross-Table Alignment Check

After T01.3 (commit 7256556) and T01.4 (this commit):

- **Precedence table** (Step 7, first table): rows now `1..15`, with Priority 14 = no-wave LG-6 → `/arc-capture`, Priority 15 = no wave, no gaps → `/arc-wave`.
- **Alternative Skill Selection Rule table** (Step 7, second table): rows now `1..15`, with `14 (no-wave LG-6) | /arc-audit (no lower-priority no-wave gap exists).` and `15 (no wave, no gaps) | /arc-audit`.

The two tables stay in lockstep on numbering and Priority 14 / 15 alignment. This is the invariant the task description requires.

## Out-of-Scope Items

- The note paragraph at line 367 (`**Never offer the recommended skill as its own alternative.**`) contains a stale reference to "Priority 14" that now points to a no-wave LG-6 row recommending `/arc-capture` rather than the old "no wave, no gaps → /arc-wave" mapping. The example in the parenthetical (`both Priority 7 and Priority 14 recommend /arc-wave`) is no longer accurate. Per the explicit task scope ("Do NOT touch rows for Priorities 1–13 in this table" and the strict spec FR6 wording about table rows only), this prose update is **not** part of T01.4. It is captured here for awareness; if a follow-up task is needed it should be filed separately.
