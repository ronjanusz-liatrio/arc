# T02.2 Proofs Summary

**Task:** T02.2 — Update SD-3 in status-dimensions.md with completed-spec exclusion rule
**Spec:** `docs/specs/18-spec-arc-status-spec-completion/18-spec-arc-status-spec-completion.md`
**Worker:** worker-2
**Completed:** 2026-05-08

## Scope

Updated `## SD-3: In-Flight Specs` in `skills/arc-status/references/status-dimensions.md` to document the completed-spec exclusion rule introduced by T02.1. Mirrors the algorithm narrative style of WL-2 / WL-3.

Edits applied:

- Extended the SD-3 **Purpose** sentence to note that already-shipped spec directories are silently excluded.
- Inserted a new step 2 in the Detection Logic numbered list that defines the **completed-spec set** as the union of two signals across `docs/skill/arc/waves/*.md`:
  - **Subsection-title signal** — `### {Title}` H3 subsection title that exactly matches a spec directory basename (case-sensitive, after whitespace trim).
  - **Spec-field signal** — `**Spec:** {path}` field whose final path component (after whitespace trim and trailing-slash normalization, the same rule used by WL-3 for `Spec:`-field comparisons) matches a spec directory basename.
- Inserted a new step 3 that documents the **silent exclusion** behavior: no count footer, no parenthetical, no separate "Completed Specs" table; the output shape is unchanged from its prior form.
- Renumbered the existing per-row artifact-detection step from 2 to 4 and the sort step from 3 to 5. The per-row artifact-detection sub-bullets (Spec File / Plan / Validation logic) are byte-for-byte preserved.
- Added a new **Match Rules (completed-spec set)** subsection mirroring the **Match Rules** subsection in WL-2 (case-sensitive, whitespace-trimmed, trailing-slash normalization on `Spec:` field values, final-path-component for `Spec:` field, no partial or substring matching).
- Extended the **Inputs** list with `docs/skill/arc/waves/*.md` for completed-spec set computation.
- Updated the **Fallback** heading parenthetical so it covers both the empty-glob case AND the empty-after-filter case (existing notice text unchanged: `No specs found — run /cw-spec to create a specification.`).

Out-of-scope sections preserved byte-for-byte:

- SD-1 and SD-2 (file lines 1-95)
- SD-3 Output Format block (the rendered markdown table example)
- SD-3 per-row artifact-detection sub-bullets (a/b/c — Spec File / Plan / Validation logic)
- SD-4 (Recent Activity)
- Lifecycle Gap Detection (LG-1..LG-6, No Gaps Detected, Error Handling)
- Wave Linkage Detection (WL-1..WL-4, Worked Example)
- Next-Step Suggestion Precedence (precedence table, evaluation logic, AskUserQuestion format, alternative-skill selection, user-selection handling, cross-references)

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | file | `T02.2-01-file.txt` | PASS |
| 2 | test | `T02.2-02-test.txt` | PASS |
| 3 | test | `T02.2-03-test.txt` | PASS |

### Proof 01: SD-3 anchor strings present

Verified all required anchor strings exist in the SD-3 region (lines 97-158):

- `## SD-3: In-Flight Specs`
- `Compute the **completed-spec set** from the wave archive`
- `Subsection-title signal`
- `Spec-field signal`
- `**union** of (b) and (c)`
- `Exclusion is **silent**`
- `Match Rules (completed-spec set)`
- `Case-sensitive`, `Whitespace-trimmed`, `Trailing-slash normalization on \`Spec:\` field values only`, `Final-path-component for \`Spec:\` field`, `No partial or substring matching`
- `docs/skill/arc/waves/*.md` — wave archive files (for completed-spec set computation) (Inputs entry)
- Updated Fallback heading covering both empty-glob and empty-after-filter cases

### Proof 02: Byte-for-byte invariants

`diff -q` between HEAD and working tree returned exit 0 for each preserved region:

- **Invariant 1** — SD-1 + SD-2 (HEAD lines 1-95 ↔ WT lines 1-95): 3066 / 3066 bytes identical.
- **Invariant 2** — SD-3 per-row artifact detection prose (HEAD lines 106-117 ↔ WT lines 112-123): 647 / 647 bytes identical.
- **Invariant 3** — SD-3 Output Format block (HEAD lines 127-135 ↔ WT lines 142-150): 187 / 187 bytes identical.
- **Invariant 4** — SD-4 + Lifecycle Gap Detection + WL sections + Next-Step Suggestion Precedence (HEAD lines 145-663 ↔ WT lines 160-678): 27198 / 27198 bytes identical.

### Proof 03: Narrative style mirrors WL-2 / WL-3

Verified SD-3 follows the established WL-2 / WL-3 narrative style:

- WL-2 has a **Match Rules:** subsection (baseline pattern present).
- SD-3 has its own **Match Rules (completed-spec set):** subsection.
- Match Rules vocabulary reused verbatim in SD-3: `Case-sensitive` (count=2), `Whitespace-trimmed` (count=2), `No partial or substring matching` (count=2).
- SD-3 Detection Logic step 2 uses lettered sub-bullets (a, b, c, d) for completed-spec set computation, mirroring WL-2's step 3.a-3.e structure.
- Trailing-slash normalization cross-references WL-3 (`the same trailing-slash normalization used by WL-3`), consistent with the LG-3/LG-4 rule.

## Files Modified

- `skills/arc-status/references/status-dimensions.md` — extended SD-3 with completed-spec set definition, exclusion rule, Match Rules subsection, Inputs entry, and updated Fallback heading parenthetical (additive only; per-row artifact detection prose, Output Format block, and all sibling sections preserved byte-for-byte).

## Files Created

- `docs/specs/18-spec-arc-status-spec-completion/18-proofs/T02.2-01-file.txt`
- `docs/specs/18-spec-arc-status-spec-completion/18-proofs/T02.2-02-test.txt`
- `docs/specs/18-spec-arc-status-spec-completion/18-proofs/T02.2-03-test.txt`
- `docs/specs/18-spec-arc-status-spec-completion/18-proofs/T02.2-proofs.md`
