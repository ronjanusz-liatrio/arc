## T01.2 Proof Summary: Compute wave-linked idea set from BACKLOG

**Task:** #4 - T01.2: Compute wave-linked idea set from BACKLOG
**Spec:** [16-spec-arc-status-wave-precedence](../16-spec-arc-status-wave-precedence.md)
**Unit:** Unit 1 - Wave-Linkage Detection and Gap Scoping
**Worker:** worker-3
**Model:** opus
**Completed:** 2026-04-24

## Scope of Change

Single-file edit to `skills/arc-status/SKILL.md` - inserted a new preparatory sub-step `#### Step 6.0: Compute Wave-Linked Idea Set (Preamble)` at the top of Step 6, immediately before the `LG-1` heading. The preamble specifies the wave-linked idea set computation (exact case-sensitive match on the BACKLOG `Wave` column, whitespace trim on both sides, null-fallback to empty set) and forward-declares the spec-to-idea linkage used by LG-3/LG-4. No changes to Step 7 - worker-4 is editing Step 7 in parallel and this edit is confined to Step 6.

## What Changed

Step 6 now opens with a preamble (Step 6.0) that documents, in the skill's operational procedure, how to compute the wave-linked idea set before running the five LG-* gap checks. The preamble:

1. Short-circuits to an empty set when the active wave name (from Step 2) is null.
2. Parses the BACKLOG summary table (same table used by Step 3).
3. Specifies the exact matching rule: trim both sides, case-sensitive string equality, no normalization.
4. Explicitly warns against substring matching, Unicode fold, and em-dash / hyphen substitution.
5. Retains the set for consumption by T01.3 (gap tagging) and T01.4 (renderer).
6. Forward-declares the spec-to-idea linkage: LG-3/LG-4 gaps look up the BACKLOG idea whose `Spec:` field equals the spec directory path; if that idea is in the wave-linked set, the gap is wave-linked - otherwise backlog-only.
7. Delegates authoritative algorithm details to `status-dimensions.md` (WL-2, WL-3).

The rendered output format of Step 6 is unchanged; only the operational procedure before the LG-* checks is augmented. The five LG checks themselves are not modified.

## Requirements Satisfied

From the spec, Unit 1 Functional Requirements:

- "The system shall compute the **wave-linked idea set** by scanning the `docs/BACKLOG.md` summary table and collecting every idea title whose `Wave` column equals the active wave name by **exact case-sensitive string match** (after trimming surrounding whitespace from both values). If active wave name is null, the wave-linked set is empty." - **covered** in Step 6.0 steps 1-4.
- "LG-3 and LG-4 resolve their subject via spec directory name; to scope these by wave, the system must look up the backlog idea linked to the spec (via a BACKLOG `Spec:` field whose value equals the spec directory path). If no such backlog idea exists, the gap is `backlog-only` by default." - **covered** in Step 6.0 spec-to-idea linkage paragraph.

From the spec, Unit 1 Proof Artifacts:

- "File: `skills/arc-status/SKILL.md` Step 6 updated to compute the wave-linked idea set and tag each gap with its scope field before emitting the table." - Step 6 is now updated to **compute** the wave-linked idea set as specified by this sub-task. The **tag each gap with its scope field** half of that bullet is deferred to T01.3 (explicitly out of scope per this sub-task's description).

## Proof Artifacts

| # | Type | File | Status | Description |
|---|------|------|--------|-------------|
| 1 | file | `T01.2-01-file.txt` | PASS | Captured diff of `skills/arc-status/SKILL.md` showing the new Step 6.0 preamble inserted between the Step 6 intro and the LG-1 heading. Diff is +19 lines, 0 deletions. |
| 2 | cli | `T01.2-02-cli.txt` | PASS | `awk '/^### Step 6/,/^### Step 7/' skills/arc-status/SKILL.md \| grep -n -E "(wave-linked idea set\|active wave name\|exact case-sensitive\|Step 6.0)"` yields 8 matches covering the heading, null-fallback, case-sensitivity rule, trim rule, and spec-to-idea linkage. |
| 3 | cli | `T01.2-03-cli.txt` | PASS | Combined validation: frontmatter parses as JSON; em dashes (`Wave 4 — Foo`) preserved verbatim (2 occurrences in preamble example text); Step 6.0 heading present exactly once at line 173; `validate-backlog.sh` and `validate-roadmap.sh` both PASS. |

## Out of Scope

- Step 2 extraction of the active wave name / status: done by **T01.1** (#3, worker-1, committed at 046db50).
- Actual tagging of each detected gap with a `scope` field: deferred to **T01.3** (#5).
- Conditional rendering of the `Scope` column in the Lifecycle Gaps table: deferred to **T01.4** (#6).
- Documentation of the wave-linkage algorithm in `status-dimensions.md`: done by **T01.5** (#7, worker-2, committed at 574dbe1) - this sub-task cross-references WL-2 and WL-3 from there.
- Step 7 precedence table rewrite: owned by **worker-4 (T02.1)** in parallel - no edits to Step 7 in this task.

## Verification Commands

Reproducible from repository root:

```bash
git diff skills/arc-status/SKILL.md
awk '/^### Step 6/,/^### Step 7/' skills/arc-status/SKILL.md | grep -n -E "(wave-linked idea set|active wave name|exact case-sensitive|Step 6.0)"
bash scripts/parse-frontmatter.sh skills/arc-status/SKILL.md --format json
bash scripts/validate-backlog.sh
bash scripts/validate-roadmap.sh
```

All commands exit with status 0 and produce the outputs recorded in the individual proof files.
