## T01.4 Proof Summary: Render conditional Scope column in Lifecycle Gaps table

**Task:** #6 - T01.4: Render conditional Scope column in Lifecycle Gaps table
**Spec:** [16-spec-arc-status-wave-precedence](../16-spec-arc-status-wave-precedence.md)
**Unit:** Unit 1 - Wave-Linkage Detection and Gap Scoping
**Worker:** worker-7
**Model:** opus
**Completed:** 2026-04-24

## Scope of Change

Single-file edit to `skills/arc-status/SKILL.md` — the existing `#### Emit Lifecycle Gaps` block (previously three-column unconditional) is rewritten to document a two-branch renderer keyed on the active wave name resolved in Step 2 (T01.1):

- **No-wave branch** (active wave name from Step 2 is null) — render the existing three-column table `Gap | Item | Remediation` unchanged; scope field ignored; skipped-check row `| {Gap Name} | (skipped — {reason}) | -- |`.
- **Active-wave branch** (active wave name from Step 2 is non-null) — render a four-column table `Gap | Item | Remediation | Scope`; Scope cell is derived from the `scope` field tagged by Step 6.6 (T01.3):
  - `wave-linked` → `Wave: {wave_name}` (verbatim active wave name; special characters pass through).
  - `backlog-only` → `Backlog (outside wave)`.
  - `--` (skipped check) → `--` (skipped-check row becomes `| {Gap Name} | (skipped — {reason}) | -- | -- |`).
- The `No lifecycle gaps detected.` message is unchanged in both branches.

No changes to:
- Step 2 extraction of active wave status — owned by **T01.1** (#3, worker-1, committed at 046db50). **Built upon, not modified.**
- Step 6.0 Preamble (wave-linked idea set) — owned by **T01.2** (#4, worker-3, committed at c1cb3b9). **Built upon, not modified.**
- LG-1 through LG-5 detection sections — unchanged.
- Step 6.6 Postamble (scope tagging) — owned by **T01.3** (#5, worker-5, committed at 7a15ba5). **Built upon, not modified.** The Emit block consumes the `scope` field that Step 6.6 produces.
- Step 7 precedence — owned by **T02.1** (committed at eaa12bc) and **T02.2** (committed at 178b7bf). Unchanged by this sub-task.
- `skills/arc-status/references/status-dimensions.md` — currently being edited in parallel by worker-8 (T02.3, #10). Per dispatch instructions I did not touch that file. Worker-8's in-flight edits to the reference remain unstaged in the shared working tree.

## What Changed

The `#### Emit Lifecycle Gaps` block (SKILL.md lines 255–299) is restructured from the pre-change three-bullet form (gaps-found-table / no-gaps-message / skipped-row-pattern) into a four-bullet form keyed on the T01.1 active wave name:

1. Preamble sentence explaining the wave-state conditional (one sentence, no behavior change for existing fields).
2. No-gaps message bullet (unchanged rendering, explicitly noted as wave-state-agnostic).
3. No-wave branch bullet — existing three-column table, skipped-row pattern unchanged.
4. Active-wave branch bullet — new four-column table, with a nested sub-list mapping each `scope` tag value to a concrete Scope cell string. Includes the skipped-check row's active-wave rendering with the extra `--` cell.
5. Trailing reminder that the `No lifecycle gaps detected.` message is never augmented.

The block now cross-references `status-dimensions.md` §WL-4 Scope Column Rendering in addition to the existing Lifecycle Gap Detection reference.

## Requirements Satisfied

From the spec, Unit 1 Functional Requirements:

- "The system shall render the Lifecycle Gaps table with an additional `Scope` column **only when an active wave exists**. Column values: For `wave-linked`: the string `Wave: {Wave Name}` (verbatim wave name). For `backlog-only`: the string `Backlog (outside wave)`." — **covered** by the active-wave branch sub-list (bullet 4) mapping `wave-linked` → `Wave: {wave_name}` and `backlog-only` → `Backlog (outside wave)`.
- "When no active wave exists, the system shall render the Lifecycle Gaps table in its existing three-column format (`Gap | Item | Remediation`) with no Scope column." — **covered** by the no-wave branch bullet (bullet 3) emitting the unmodified three-column table.
- "Skipped checks shall continue to render as `| (skipped — reason) | -- |` with the Scope column populated as `--` when it is present." — **covered** by both branches' explicit skipped-row rendering: no-wave branch `| ... | (skipped — {reason}) | -- |` (three cells) and active-wave branch `| ... | (skipped — {reason}) | -- | -- |` (four cells).
- "The existing `No lifecycle gaps detected.` message shall remain unchanged." — **covered** by bullet 2 (wave-state-agnostic) and reinforced by the trailing explicit reminder bullet (bullet 5).

From the Design Considerations:

- "Wave name with special markdown characters (e.g., em dashes, colons) must pass through verbatim in both the table and the `AskUserQuestion` reason template so users see the exact roadmap string." — **covered** by the explicit verbatim-passthrough clause in the active-wave branch ("no escaping, no trimming, no case folding. Special characters (em dashes, colons, etc.) pass through unchanged (e.g., `Wave: Wave 4 — Foo`)").
- "Skipped-check rows inherit `--` in the Scope column when rendered in active-wave mode — scope cannot be computed for a check that failed to execute." — **covered** by the active-wave skipped-row clause.

From Unit 1 Proof Artifacts:

- "File: `skills/arc-status/SKILL.md` Step 6 updated to compute the wave-linked idea set and tag each gap with its scope field before emitting the table." — the **compute** half is T01.2 (committed), **tag** is T01.3 (committed), and the **emit with conditional Scope column** half is satisfied by this sub-task.

## Proof Artifacts

| # | Type | File | Status | Description |
|---|------|------|--------|-------------|
| 1 | file | `T01.4-01-file.txt` | PASS | Captured `git diff skills/arc-status/SKILL.md` showing single-hunk replacement of the Emit Lifecycle Gaps block (+24/-9 lines). Confirms all changes are scoped to lines 255–284 of HEAD; no drift into Step 2, Step 6.0, LG-1..5, Step 6.6, or Step 7. |
| 2 | cli | `T01.4-02-cli.txt` | PASS | Content match via `awk` + `grep -E` for the eight required render strings: both table headers (`Gap \| Item \| Remediation` and `Gap \| Item \| Remediation \| Scope`), both Scope cell formats (`Wave: {wave_name}` and `Backlog (outside wave)`), both skipped-check renderings (three-cell and four-cell), the preserved `No lifecycle gaps detected.` message, and both conditional-branch markers (`active wave name from Step 2 is null` / `... is non-null`). Found 10 matches (threshold ≥ 8). |
| 3 | cli | `T01.4-03-cli.txt` | PASS | Structural preservation: Step 6 sub-step ordering remains `Step 6 (167) → Step 6.0 (173) → LG-1..5 (192..225) → Step 6.6 (234) → Emit (255) → Step 7 (301)`. Step 6.0 (worker-3) and Step 6.6 (worker-5) are untouched. Frontmatter parses as JSON; `validate-backlog.sh` and `validate-roadmap.sh` both PASS. |

## Parallel-Worker Coordination

- **Worker-1 (T01.1 Step 2 Active Wave Status):** Already committed at 046db50. The Emit block's conditional (active wave name from Step 2) consumes the value Step 2 makes available. No modification to Step 2.
- **Worker-3 (T01.2 Step 6.0 Preamble):** Already committed at c1cb3b9. The Emit block's cross-reference to `status-dimensions.md` and the implicit dependency on the wave-linked idea set are inherited from Step 6.0. No modification to Step 6.0 — verified at line 173 unchanged.
- **Worker-5 (T01.3 Step 6.6 Postamble):** Already committed at 7a15ba5. The Emit block's active-wave branch consumes the `scope ∈ { wave-linked, backlog-only, -- }` field that Step 6.6 tags onto each gap. No modification to Step 6.6 — verified at line 234 unchanged.
- **Worker-2 (T01.5 status-dimensions.md WL-4):** Already committed at 574dbe1. The Emit block's authoritative reference for rendering rules points to WL-4 in that file.
- **Worker-8 (T02.3 status-dimensions.md Next-Step Precedence):** In flight in parallel on `skills/arc-status/references/status-dimensions.md`. Per dispatch instructions, I restricted my edits to `skills/arc-status/SKILL.md` and did not touch `status-dimensions.md`. Worker-8's in-flight modifications to that file remain unstaged in the shared working tree and will not be included in this commit — verified via `git diff --cached --stat` (only `skills/arc-status/SKILL.md` is staged). Only my SKILL.md change is committed.

## Out of Scope

- Mutation of the `skills/arc-status/references/status-dimensions.md` §WL-4 reference content: that section is already present (authored by worker-2 in T01.5 at 574dbe1); this sub-task's SKILL.md Emit block cross-references WL-4 as the authoritative source but does not modify it.
- Step 7 consumption of the `scope` field to suppress backlog-only gaps from Priorities 2–6: owned by T02.1 (committed at eaa12bc) and T02.2 (committed at 178b7bf).
- Concrete fixtures/test runs of `/arc-status` against crafted projects: the spec's "Non-Goals" explicitly defers a test harness to future work. The CLI-outcome proof artifacts listed in the spec (e.g., "run on a project with an active wave named `Wave 4 — Foo` ...") are specification-level acceptance criteria that would be executed when running the actual skill against a crafted project; they are not implementation-time test requirements for this sub-task.

## Verification Commands

Reproducible from repository root:

```bash
git diff skills/arc-status/SKILL.md
awk '/^#### Emit Lifecycle Gaps/,/^### Step 7:/' skills/arc-status/SKILL.md | grep -n -E 'Gap \| Item \| Remediation \| Scope|Gap \| Item \| Remediation \||Wave: \{wave_name\}|Backlog \(outside wave\)|No lifecycle gaps detected|active wave name from Step 2 is null|active wave name from Step 2 is non-null|\(skipped — \{reason\}\) \| -- \| -- \||\(skipped — \{reason\}\) \| -- \|'
grep -n '^#### Step\|^#### LG-\|^#### Emit\|^### Step 6\|^### Step 7' skills/arc-status/SKILL.md
bash scripts/parse-frontmatter.sh skills/arc-status/SKILL.md --format json >/dev/null
bash scripts/validate-backlog.sh
bash scripts/validate-roadmap.sh
```

All commands exit with status 0 and produce the outputs recorded in the individual proof files.
