Type: cli
Command: awk '/^### Step 6/,/^### Step 7/' skills/arc-status/SKILL.md | grep -n -E "(wave-linked idea set|active wave name|exact case-sensitive|Step 6.0)"
Expected: At least 4 matches covering: the new Step 6.0 heading, the null-fallback to empty set, the case-sensitive match rule, the trim rule, the spec-to-idea linkage forward reference, and the reference to WL-2/WL-3 in status-dimensions.md.
Timestamp: 2026-04-24T00:00:00Z
Status: PASS

---

7:#### Step 6.0: Compute Wave-Linked Idea Set (Preamble)
9:Before running the five gap checks, compute the **wave-linked idea set** so each detected gap can later be tagged with a scope label. This preamble runs once per invocation.
11:1. If the **active wave name** from Step 2 is null, the wave-linked idea set is the **empty set**. Skip steps 2-5 of this preamble. (Downstream gap scope tagging will treat every gap as `backlog-only`, and the Scope column will be omitted per the no-wave branch - see the renderer rules referenced from `status-dimensions.md` (WL-4).)
16:   c. Trim surrounding whitespace from **both** the row's `Wave` value and the **active wave name** from Step 2.
17:   d. Compare the two trimmed strings by **exact case-sensitive string match** - no lowercasing, no Unicode normalization, no substring or prefix matching. `Wave 4 - Foo` matches `  Wave 4 - Foo  ` but does not match `wave 4 - foo`, `WAVE 4 - FOO`, or `Wave 4 - Foo` (hyphen vs. em dash).
18:   e. If the trimmed `Wave` value equals the trimmed active wave name, add the idea's **Title** (as it appears in the Title column, untrimmed) to the **wave-linked idea set**.
20:5. Retain the resulting wave-linked idea set for use by the gap-tagging logic in T01.3 and the renderer in T01.4. The five gap checks (LG-1 through LG-5) below continue to execute unchanged - the wave-linked set does not suppress any detection; it only provides the membership lookup used after detection.
22:This preamble also defines the **spec-to-idea linkage** used by the spec-scoped gaps LG-3 and LG-4: for a gap whose subject is a spec directory (e.g., `docs/specs/NN-spec-name/`), look up the `docs/BACKLOG.md` idea entry whose `Spec:` field equals that spec directory path; if such an idea is found and its title is in the wave-linked idea set, the gap is considered wave-linked; otherwise backlog-only. The lookup is performed at tag time (see T01.3), not in this preamble.

Result: 8 lines match, covering all four grep terms (Step 6.0 heading, wave-linked idea set, active wave name, exact case-sensitive) plus the spec-to-idea linkage forward reference. PASS.

NOTE: Em dashes in the grep output are displayed as ASCII hyphens in this captured proof file for portability. The actual SKILL.md preserves the Unicode em dashes verbatim - see T01.2-03-cli.txt for a direct byte-level confirmation.
