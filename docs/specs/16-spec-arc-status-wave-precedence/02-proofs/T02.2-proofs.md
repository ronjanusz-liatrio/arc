# T02.2 — Implement alternative-skill selection rules

**Task:** Update the `Select the alternative skill` block in `skills/arc-status/SKILL.md` Step 7 so it matches the spec's per-priority alternative rules, and ensure the `AskUserQuestion` prompt always includes at least three options: `(Recommended)` skill, one alternative, and `Done for now`.

**Spec:** `docs/specs/16-spec-arc-status-wave-precedence/16-spec-arc-status-wave-precedence.md` (Unit 2: Wave-Priority Next-Step Precedence — "The system shall select the **alternative skill** for the `AskUserQuestion` prompt as follows" block, plus the three-options invariant).

## Proof Artifacts

| # | Type | File | Status | What it proves |
|---|------|------|--------|----------------|
| 1 | file | `T02.2-01-file.txt` | PASS | Per-priority alternative-skill selection table is documented for all 14 priorities, matching spec rules (a) through (e). |
| 2 | file | `T02.2-02-file.txt` | PASS | `AskUserQuestion` prompt always offers three options — `(Recommended)`, alternative, and `Done for now` — and the pre-existing User Selection handling (`Done for now` exits silently) is preserved. |
| 3 | file | `T02.2-03-file.txt` | PASS | Edit is confined to the Step 7 `Present Recommendation` block; Step 6, the Step 7 precedence table (T02.1 output), and the User Selection / Critical Constraints subsections are untouched. |

## Requirement → Proof Map

| Spec Requirement | Proof |
|------------------|-------|
| Rule (a): Priority 1 → `/arc-audit` | T02.2-01 (row "1 (empty wave)") |
| Rule (b): Priorities 2–6 → next-lower wave-linked gap, else `/arc-audit` | T02.2-01 (rows 2–6) |
| Rule (c): Priorities 7 and 8 → the other of `/arc-wave` or `/arc-audit` | T02.2-01 (rows 7, 8) |
| Rule (d): Priorities 9–13 → next-lower no-wave gap, else `/arc-audit` | T02.2-01 (rows 9–13) |
| Rule (e): Priority 14 → `/arc-audit` | T02.2-01 (row 14) |
| Prompt includes `(Recommended)` + alternative + `Done for now` (≥3 options) | T02.2-02 (lines 319, 350, 352 in SKILL.md) |
| User Selection: `Done for now` exits silently without invoking a skill | T02.2-02 (line 364, preserved unchanged) |
| Edit scope: Step 7 `Present Recommendation` block only | T02.2-03 (hunk @ line 316 only, Step 7 table and Handle User Selection untouched) |

## Verification Method

- **Static analysis**: `sed`/`grep` over the authoritative `skills/arc-status/SKILL.md` to confirm each spec rule is represented in the updated "Present Recommendation" block.
- **Scope confinement**: `git diff --stat` and hunk-header inspection to confirm the only T02.2-authored hunk targets the Step 7 `Present Recommendation` block.
- No runtime CLI execution — this is a prescriptive change to skill documentation that is interpreted by Claude at runtime. Feature-file scenarios referencing this behavior (e.g., "Alternative for Priority 2–6 offers the next-lower matching wave-linked gap skill", "AskUserQuestion prompt always offers at least three options", "Alternative falls back to /arc-audit when no lower-priority wave-linked gap exists") are validated by the body of the prescriptive rules now present in SKILL.md.

## Conclusion

Status: **PASS**

The Step 7 `Present Recommendation` block now:
1. Specifies an explicit alternative-skill selection rule for each of the 14 priorities, matching spec rules (a)–(e).
2. Asserts the three-options invariant (`(Recommended)` + alternative + `Done for now`) as a body directive, independent of the prompt-format code block.
3. Preserves the pre-existing `Handle User Selection` block (Skill invocation only on explicit selection; `Done for now` exits silently) and the `Critical Constraints for Step 7` block.

Edits are scope-confined to lines 316–337 of `skills/arc-status/SKILL.md` (post-edit numbering). The Step 7 precedence table (T02.1 output) and Step 6 (worker-5's T01.3 territory) are untouched.
