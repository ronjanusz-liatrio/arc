# T01.5 Proof Summary

**Task:** Document wave-linkage algorithm in `status-dimensions.md`
**Status:** COMPLETED
**Timestamp:** 2026-04-24T11:20:45Z

## Scope

Added a new `## Wave Linkage Detection` section to `skills/arc-status/references/status-dimensions.md`, placed after `## Lifecycle Gap Detection` and before `## Next-Step Suggestion Precedence` so the ordering matches Step 6 → Step 7 reasoning.

The new section contains four sub-dimensions and a worked example:

- **WL-1: Active Wave Resolution** — documents the first-non-Completed-row rule, verbatim wave name extraction, active wave status extraction (`planned`/`active`), and the `(null, null)` fallback when no eligible row exists.
- **WL-2: Wave-Linked Idea Set** — documents exact case-sensitive match with surrounding-whitespace trim on the BACKLOG `Wave` column, negative match rules (internal whitespace, hyphen vs. em dash, substring matching), and the empty-set fallback when the active wave is null.
- **WL-3: Gap Scope Tagging** — documents how each LG-1..LG-5 gap is tagged `wave-linked`, `backlog-only`, or `--` (for skipped checks). Includes spec-to-idea linkage for LG-3/LG-4 via the BACKLOG `Spec:` field.
- **WL-4: Scope Column Rendering** — documents the conditional four-column vs. three-column table rendering and the `Wave: {wave_name}`, `Backlog (outside wave)`, and `--` literal cell values.
- **Worked Example** — three-idea BACKLOG against wave name `Wave 4 — Foo`, showing wave-linked vs. backlog-only outcomes (including the whitespace-trim edge case). Contrasts with the null-active-wave scenario.

## Files Modified

- `skills/arc-status/references/status-dimensions.md` — added lines 369-515 (new `## Wave Linkage Detection` section, its four sub-headings, and the worked example), no other content changed.

## Proof Artifacts

| # | File | Type | Status | Purpose |
|---|------|------|--------|---------|
| 1 | `T01.5-01-file.txt` | file | PASS | Verifies new section and sub-headings exist |
| 2 | `T01.5-02-file.txt` | file | PASS | Verifies each required content element from task description is present |
| 3 | `T01.5-03-file.txt` | file | PASS | Verifies required section ordering (Lifecycle Gap Detection → Wave Linkage Detection → Next-Step Suggestion Precedence) |

## Proof Results

- **Proof 1 (Section structure):** PASS — `## Wave Linkage Detection` heading present at line 371 with sub-headings WL-1 through WL-4 and a Worked Example.
- **Proof 2 (Content completeness):** PASS — all seven required content elements (active wave resolution, wave name + status extraction, exact case-sensitive match, whitespace trim, null-wave fallback, spec-to-idea linkage, `--` scope for skipped checks, worked example) are verifiable at specific line numbers in the edited file.
- **Proof 3 (Section ordering):** PASS — new section placed at line 371, after Lifecycle Gap Detection (line 181) and before Next-Step Suggestion Precedence (line 517).

## Notes

This task only adds documentation to a reference file. No code, tests, or skill-invocation behavior is affected. Downstream tasks T01.1–T01.4 (owned by worker-1 and other future workers) will reference this section when implementing Step 2 (active wave status extraction), Step 6 (gap scope tagging + Scope column rendering), and Step 7 (wave-priority precedence).

The task explicitly blocks task #1 (T01 rollup). Task #1 remains pending until all T01.x subtasks (including this one, T01.1, T01.2, T01.3, T01.4) complete.

The documentation is authoritative: it matches the functional requirements in `16-spec-arc-status-wave-precedence.md` for Unit 1 and aligns with the acceptance scenarios in `wave-linkage-detection-and-gap-scoping.feature`.
