# T02.1 Proof Summary: Rewrite Step 7 precedence table with 14 priorities

**Task:** #8 — T02.1: Rewrite Step 7 precedence table with 14 priorities
**Spec:** [16-spec-arc-status-wave-precedence](../16-spec-arc-status-wave-precedence.md)
**Unit:** Unit 2 — Wave-Priority Next-Step Precedence
**Worker:** worker-4
**Model:** opus
**Completed:** 2026-04-24

## Scope of Change

Single-file edit to `skills/arc-status/SKILL.md` Step 7. The 7-row Precedence List and its one-line introduction are replaced with a 14-row wave-state-aware precedence table, an expanded introduction that names the wave/no-wave branch cutoff, and three new clarifier paragraphs covering the P0/P1 filter, backlog-only gap suppression, and wave-name interpolation. The rest of Step 7 (`#### Present Recommendation`, `#### Handle User Selection`, `#### Critical Constraints for Step 7`) is intentionally unchanged — the alternative-skill selection rules still reference P1–P5/P6–P7 and will be updated by task #9 (T02.2). Step 6 is untouched by this task (worker-3 is editing Step 6 in parallel under task T01.2).

## What Changed

1. **Introduction paragraph** — rewritten to describe a wave-state-aware list: Priorities 1–8 (active wave) and Priorities 9–14 (no active wave).
2. **Precedence List** — replaced with the 14-row table defined in the spec (rows 119–132). First-match-wins evaluation from Priority 1 downward is preserved. Each row's Recommended Skill and Reason Template match the spec verbatim, including the wave-name substitution token `{Name}`.
3. **Clarifier paragraphs** — three new paragraphs added after the table:
   - **Priority 6 / Priority 13 P0/P1 filter** — documents that LG-1 gaps must have `Priority: P0` or `P1` in the BACKLOG metadata block to trigger Priority 6 or Priority 13, matching the pre-change Priority-5 filter behavior (spec FR rows 150–152).
   - **Wave scope and backlog-only gaps** — documents that backlog-only gaps never satisfy Priorities 2–6 and are not considered by Priorities 7/8, but can match Priorities 9–13 when the wave is null (spec FR rows 144–146).
   - **Wave-name interpolation** — documents verbatim wave-name pass-through in reason templates, no escaping beyond standard JSON string encoding (spec FR rows 153–156).

The trailing reference line (`See skills/arc-status/references/status-dimensions.md (Next-Step Suggestion Precedence) for the full precedence logic.`) is retained unchanged so the existing link to the reference file is preserved. The reference file itself (`status-dimensions.md`) still shows the legacy 7-row table — that rewrite is task #10 (T02.3).

## Requirements Satisfied

From spec Unit 2 Functional Requirements:

- "replace the existing Step 7 precedence with the following table, evaluated first-match-wins from Priority 1 downward" (spec line 114) — **covered** in the Precedence List (14 rows, PASS per T02.1-02-cli.txt).
- "Backlog-only gaps do not satisfy Priorities 2–6 and are not considered by Priorities 7 and 8" (spec lines 144–146) — **covered** in the **Wave scope and backlog-only gaps** clarifier paragraph.
- "The Priority 6 P0/P1 filter shall read the `Priority:` field from the idea's metadata block in `docs/BACKLOG.md`, matching the existing Priority-5 filter behavior" (spec lines 150–152) — **covered** in the **Priority 6 / Priority 13 P0/P1 filter** clarifier paragraph.
- "the system shall pass the wave name verbatim — no escaping transformations beyond standard JSON string encoding" (spec lines 153–156) — **covered** in the **Wave-name interpolation** clarifier paragraph.

From spec Unit 2 Proof Artifacts (T02.1 slice):

- "File: `skills/arc-status/SKILL.md` Step 7 updated to document the new 14-row precedence table, … and the wave-name interpolation for reason templates." — **covered** by `T02.1-01-file.txt` (Step 7 hunk diff) and `T02.1-02-cli.txt` (row-by-row match against spec rows 119–132).

Out-of-scope items left for successor tasks (tracked in `T02.1-03-cli.txt`):

- **Alternative-skill selection rules** for Priorities 1, 2–6, 7/8, 9–13, 14 — task **#9 T02.2**.
- **`status-dimensions.md` Next-Step Suggestion Precedence section rewrite** — task **#10 T02.3**.
- **CLI fixture proofs** (Priority 6 planned-wave case, Priority 8 clean-active-wave case, Priority 1 empty-wave case, Priority 12 no-wave LG-2 case) — best executed end-to-end after T02.2 and T02.3 land, so that the prompt uses the corrected alternative skills. They are called out in the spec's Unit 2 Proof Artifacts section for the full Unit 2 acceptance run.

## Proof Artifacts

| # | Type | File | Status | Description |
|---|------|------|--------|-------------|
| 1 | file | `T02.1-01-file.txt` | PASS | Step 7 hunk from `git diff skills/arc-status/SKILL.md` showing the before/after of the introduction paragraph, the full 14-row Precedence List, and the three new clarifier paragraphs. Attribution note explains that Step 6.0 hunks in the working tree belong to worker-3 (T01.2) and will be committed separately. |
| 2 | cli | `T02.1-02-cli.txt` | PASS | `awk /### Step 7/,/### Step 8\|## References/ \| grep -nE '^\| [0-9]+ \|'` yields exactly 14 priority rows. Each row is cross-checked line-by-line against spec rows 119–132 (Recommended Skill and branch). All 14 match. |
| 3 | cli | `T02.1-03-cli.txt` | PASS | Clarifier-paragraph presence check (`grep -c` for the three bold paragraph headings) returns 3. Requirement-by-requirement checklist confirms all 5 Unit 2 FRs in T02.1's scope are documented. Scope boundary checks document what T02.1 deliberately did NOT change (Step 6, Present Recommendation block, status-dimensions.md). Baseline `validate-backlog`, `validate-roadmap`, and `parse-frontmatter` all PASS post-edit. |

## Out of Scope

- Step 6 and Step 6.0 preamble (worker-3, task #4 T01.2).
- Step 7 "Present Recommendation" block — alternative-skill selection rules (task #9 T02.2).
- `skills/arc-status/references/status-dimensions.md` Next-Step Suggestion Precedence section (task #10 T02.3).
- Step 7 "Handle User Selection" block — preserved unchanged.
- Step 7 "Critical Constraints for Step 7" block — preserved unchanged.
- CLI fixture acceptance runs — deferred to Unit 2 final acceptance after T02.2 + T02.3 land.

## Verification Commands

Reproducible from repository root:

```bash
git diff skills/arc-status/SKILL.md
awk '/^### Step 7/,/^### Step 8|^## References/' skills/arc-status/SKILL.md | grep -nE '^\| [0-9]+ \|' | wc -l
awk '/^### Step 7/,/^### Step 8|^## References/' skills/arc-status/SKILL.md | grep -cE '^\*\*Priority 6 / Priority 13 P0/P1 filter\.\*\*|^\*\*Wave scope and backlog-only gaps\.\*\*|^\*\*Wave-name interpolation\.\*\*'
bash scripts/validate-backlog.sh
bash scripts/validate-roadmap.sh
bash scripts/parse-frontmatter.sh skills/arc-status/SKILL.md --format json
```

Expected exits: all exit 0. Row-count command prints `14`. Clarifier-paragraph command prints `3`. Validators print PASS.
