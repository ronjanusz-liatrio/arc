# T06.1 — Proof Summary

**Task:** Seed 9 trigger-eval JSON files (10 trigger + 10 no-trigger each)
**Spec scope:** `tests/trigger-evals/<skill>.json` for the 9 arc skills
**Feature file:** `docs/specs/15-spec-agent-utilization-upgrade/trigger-eval-harness.feature`

## Artifacts created

| File | Queries | should-trigger | should-NOT-trigger |
|---|---|---|---|
| `tests/trigger-evals/arc-capture.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-shape.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-wave.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-ship.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-status.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-audit.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-assess.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-sync.json` | 20 | 10 | 10 |
| `tests/trigger-evals/arc-help.json` | 20 | 10 | 10 |
| **Totals** | **180** | **90** | **90** |

## Proof artifacts

| # | File | Scenario | Status |
|---|---|---|---|
| 01 | `T06.1-01-cli.txt` | 1 — Nine `<skill>.json` files exist under `tests/trigger-evals/` | PASS |
| 02 | `T06.1-02-cli.txt` | 2 + 3 — `jq` group_by yields `[{key:false,count:10},{key:true,count:10}]` for every file (includes arc-shape) | PASS |
| 03 | `T06.1-03-cli.txt` | 4 + 5 — `jot this down` appears in arc-capture should-trigger; `audit the pipeline` near-miss appears in arc-status should-NOT-trigger and is annotated to arc-audit | PASS |

## Feature coverage

| Scenario | Evidence |
|---|---|
| Scenario 1: Eval file exists for every arc skill | `T06.1-01-cli.txt` lists 9 files. |
| Scenario 2: Each file contains exactly 20 queries split 10/10 | `T06.1-02-cli.txt` jq output identical for every skill. |
| Scenario 3: arc-shape includes the required 10/10 split | `T06.1-02-cli.txt` row for arc-shape. |
| Scenario 4: ≥3 informal phrasings in should-trigger per skill | See "Informal phrasings per skill" below. |
| Scenario 5: ≥3 sibling near-misses in should-NOT-trigger per skill | See "Sibling near-misses per skill" below. |
| Scenarios 6-8: runner doc + baselines + aggregate summary | Out of scope for T06.1 — handled by T06.2 (#29), T06.3 (#30), T06.4 (#31). |

## Informal phrasings per skill (scenario 4, ≥3 each)

| Skill | Informal / adjacent phrasings among the 10 should-trigger queries |
|---|---|
| arc-capture | "Jot this down before I forget…"; "Add to the list…"; "Stick this on the backlog…"; "Quick thought —…"; "Log this:…"; "Can you note that…" |
| arc-shape | "Flesh out the SSO idea…"; "Work up a proper brief…"; "Let's dig into… and size it up"; "Tighten up the onboarding-tour idea…" |
| arc-wave | "Let's line up what we're building this sprint"; "Bundle the top three shaped items…"; "Pick what we're doing next round…" |
| arc-ship | "Wrap this one up — cw-validate said PASS"; "Cross it off…"; "Put the SSO feature to bed…"; "Close out the finished work…" |
| arc-status | "How are we doing across the pipeline right now?"; "Give me the 30-second read on the project"; "Where are we at on the current wave?" |
| arc-audit | "Something's off with the backlog — find what"; "Run the health check across our product artifacts"; "Look for stale ideas and broken roadmap refs" |
| arc-assess | "We just installed Arc — pull in whatever product stuff is already in the repo"; "Round up the TODOs and feature lists…"; "Sweep the project for product-direction notes…" |
| arc-sync | "The README is stale — make it match…"; "Bring the README up to date before I share the repo"; "Refresh the managed sections in README.md" |
| arc-help | "What can arc do?"; "Help — where do I even start with this plugin?"; "Give me the cheat sheet for all the /arc commands"; "New to Arc — show me the overview" |

## Sibling near-misses per skill (scenario 5, ≥3 each)

| Skill | Near-misses and their intended sibling routing |
|---|---|
| arc-capture | "Let me shape this idea" → arc-shape; "What's the backlog status?" → arc-status; "Refine the SSO idea into a spec-ready brief" → arc-shape; "Audit the backlog for stale captured ideas" → arc-audit; "Show me what's in the backlog right now" → arc-status; "Scan the repo for scattered TODOs and bring them into Arc" → arc-assess; "Ship the dark-mode feature now that validation passed" → arc-ship; "Plan the next wave with these four ideas" → arc-wave |
| arc-shape | "Just record this quick thought" → arc-capture; "Write the detailed spec for this feature" → /cw-spec; "Plan the next wave with the already-shaped ideas" → arc-wave; "Audit the pipeline for incomplete briefs" → arc-audit; "Jot down the idea the user just asked about" → arc-capture; "Ship this already-validated feature to archive" → arc-ship; "What's the status of the pipeline?" → arc-status; "Update the README…" → arc-sync |
| arc-wave | "What's the current status?" → arc-status; "Write specs for the next wave ideas" → /cw-spec; "Shape the 'dark mode' idea before we plan anything" → arc-shape; "Capture this new feature request…" → arc-capture; "Audit wave alignment…" → arc-audit; "Ship the completed ideas from wave 2…" → arc-ship; "Sync the README with the latest wave archive entry" → arc-sync |
| arc-ship | "Write a validation report" → /cw-validate; "What's pipeline health?" → arc-status; "Audit whether any spec-ready ideas…" → arc-audit; "Capture a new idea — ship tracking dashboard" → arc-capture; "Plan the next wave…" → arc-wave; "Shape this customer request…" → arc-shape; "Update the README with the shipped features…" → arc-sync; "Run the cw-validate pass on this spec" → /cw-validate |
| arc-status | "Audit the pipeline and fix issues" → arc-audit; "Update the README" → arc-sync; "Audit the pipeline for stale ideas" → arc-audit; "Check brief completeness across the shaped ideas" → arc-audit; "Capture a new idea — status dashboard widget" → arc-capture; "Shape the 'status page' idea…" → arc-shape; "Plan the next wave" → arc-wave; "Ship the completed ideas…" → arc-ship |
| arc-audit | "Quick status check" → arc-status; "Audit engineering gates" → Temper (external); "Show me a 30-second project overview" → arc-status; "What's the status of the current wave?" → arc-status; "Capture this issue…" → arc-capture; "Shape the 'audit dashboard' idea…" → arc-shape; "Plan the next wave…" → arc-wave; "Sync the README with the latest audit findings" → arc-sync |
| arc-assess | "Record this new idea" → arc-capture; "Update the README" → arc-sync; "Audit the backlog for stale items" → arc-audit; "Capture a single new idea…" → arc-capture; "Shape the top three captured ideas…" → arc-shape; "Plan the next wave…" → arc-wave; "What's the current pipeline status?" → arc-status; "Scaffold a README from the Arc artifacts" → arc-sync |
| arc-sync | "Consolidate scattered backlog items" → arc-assess; "What's the current status?" → arc-status; "Audit the README for broken ROADMAP references" → arc-audit; "Capture a new idea…" → arc-capture; "Shape the 'project homepage' idea…" → arc-shape; "Plan the next wave…" → arc-wave; "Ship the README-refresh feature…" → arc-ship; "Scan the repo for scattered product content…" → arc-assess |
| arc-help | "What's the status of our current wave?" → arc-status; "Audit the backlog for me" → arc-audit; "Can you help me shape this 'dark mode' idea?" → arc-shape; "Help me capture a bunch of ideas…" → arc-capture; "Help plan the next wave…" → arc-wave; "Could you walk me through shipping this validated feature?" → arc-ship; "Help me update the README" → arc-sync; "What does the backlog look like right now?" → arc-status |

Per open question #4 in the spec, arc-help near-misses are deliberately "looks-like-help but should route elsewhere" — sentences of the form "help me {substantive verb}" route to the substantive skill, not to arc-help.

## Appendix A seed coverage

Every Appendix A seed utterance from `research-agent-utilization.md` is preserved verbatim in the corresponding eval file and annotated in each query's rationale field. The remaining queries (7 trigger + 8 no-trigger per skill) were authored as expansions of those seeds per Anthropic's 20-query eval methodology.
