# T04.2 Proof Summary — Author 4 JSON Schemas under schemas/

**Task:** T04.2 (native #19) — Author 4 JSON Schemas under `schemas/`.
**Completed:** 2026-04-22
**Model:** opus

## Deliverables

Four JSON Schema Draft-07 files created under `schemas/`:

- `schemas/backlog-entry.schema.json` — one BACKLOG idea; requires brief fields when `status` is `shaped` or `spec-ready`.
- `schemas/brief.schema.json` — stand-alone spec-ready brief (problem, proposed_solution, success_criteria >=3, constraints, assumptions, wave_assignment, open_questions).
- `schemas/wave.schema.json` — wave record shared by ROADMAP waves and `docs/skill/arc/waves/` archive files; requires `completed` when `status` is `Completed`.
- `schemas/roadmap-row.schema.json` — single row of the ROADMAP Wave Summary Table; enforces `^Wave \d+: .+$` and restricts status to `Planned`/`Active` (Completed waves are archived, not rendered here).

All four schemas:
- Declare `$schema: "http://json-schema.org/draft-07/schema#"`, `$id`, `title`, `description`, `type`, `required`, `properties` per the task description.
- Compile as valid Draft-07 under `ajv-cli@5` + `ajv-formats@2`.
- Validate cleanly against representative real artifacts extracted from the current repository (`docs/BACKLOG.md`, `docs/skill/arc/waves/03-wave-lifecycle-closure.md`, and a ROADMAP row shape from `docs/ROADMAP.md`/`templates/ROADMAP.tmpl.md`).
- Reject malformed instances with specific, actionable diagnostics (negative-case spot checks).

## Proof Artifacts

| # | File | Type | Status |
|---|------|------|--------|
| 01 | `T04.2-01-compile.txt` | cli | PASS — all 4 schemas compile as valid Draft-07 |
| 02 | `T04.2-02-validate-real.txt` | cli | PASS — each schema validates its representative real artifact |
| 03 | `T04.2-03-negative-spotcheck.txt` | cli | PASS — each schema fails malformed input with a specific diagnostic |

## Notes

- The research report §5.3 is the source of shape decisions. Where the report left flexibility (e.g. backlog entries that move to `shaped`/`spec-ready` must expand to brief fields), the schemas make the conditional required-ness explicit using `allOf`/`if`/`then`.
- `ajv-formats` is required at validation time for the `format: "date-time"` keyword — this CLI decision belongs to T04.1 (#18) and is recorded here for the downstream validator scripts (#20, #21).
- No validators, no `schemas/tests/` fixtures, and no state predicate were implemented — those are tasks #20, #21, #22, #23 and remain blocked by this task until now.

## Files Touched

- `schemas/backlog-entry.schema.json` (created)
- `schemas/brief.schema.json` (created)
- `schemas/wave.schema.json` (created)
- `schemas/roadmap-row.schema.json` (created)
- `docs/specs/15-spec-agent-utilization-upgrade/04-proofs/T04.2-01-compile.txt` (created)
- `docs/specs/15-spec-agent-utilization-upgrade/04-proofs/T04.2-02-validate-real.txt` (created)
- `docs/specs/15-spec-agent-utilization-upgrade/04-proofs/T04.2-03-negative-spotcheck.txt` (created)
- `docs/specs/15-spec-agent-utilization-upgrade/04-proofs/T04.2-proofs.md` (this file)
