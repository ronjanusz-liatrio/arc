# T04.3 Proof Summary — scripts/validate-backlog.sh

Task: T04.3 — Implement scripts/validate-backlog.sh
Completed: 2026-04-23
Model: sonnet

## Proof Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T04.3-01-validate-pass.txt | cli | PASS | Script exits 0 on current docs/BACKLOG.md (20 entries validated) |
| T04.3-02-shellcheck-and-mode.txt | cli | PASS | ShellCheck severity=style exits 0; file is chmod 0755 |

## Summary

`scripts/validate-backlog.sh` was created and validated against the live BACKLOG.md.

- Parses the BACKLOG.md summary table and all `## {Title}` sections into NDJSON
- Validates each entry against `schemas/backlog-entry.schema.json` via ajv-cli@5 + ajv-formats@2
- Enforces three structural invariants: table/section consistency, no duplicate titles,
  shaped/spec-ready entries must have a `brief` field
- Exit codes: 0 pass, 1 fail, 2 recoverable (reserved)
- ShellCheck clean at severity=style; chmod 0755; non-interactive, chainable

All 20 current BACKLOG entries pass schema validation.
