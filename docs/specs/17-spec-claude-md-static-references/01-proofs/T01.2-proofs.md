# T01.2 Proof Summary

**Task:** Remove Update Behavior table and add migration + idempotency narrative
**Spec:** `docs/specs/17-spec-claude-md-static-references/17-spec-claude-md-static-references.md` (Unit 1)
**File modified:** `skills/arc-wave/references/bootstrap-protocol.md`
**Status:** COMPLETED
**Timestamp:** 2026-04-27T00:00Z

## Scope

Replace the legacy `## Update Behavior` section in `skills/arc-wave/references/bootstrap-protocol.md` (heading + intro sentence "Each /arc-wave invocation updates..." + the 5-row Field/Source/Fallback table mapping Vision/Phase/Current Wave/Primary Personas/Backlog to data sources + the closing single-sentence idempotency clause) with a renamed `## Migration and Idempotency` section that documents:

(a) the bootstrap protocol replaces all content between `<!--# BEGIN ARC:product-context -->` and `<!--# END ARC:product-context -->` with the static template regardless of prior content (live counts, already-migrated static, hand-edited text, anything else); migration and routine writes share one code path with no legacy-detection branch.

(b) the protocol is idempotent — running it twice produces byte-identical bytes between marker positions because the template is fixed (no derived values, no timestamps).

The new section's location is preserved relative to `## Coexistence Rules` (above) and `## Cross-References` (below) — only the content of this single section changes.

## Out of Scope (Other T01 Subtasks)

- Replacing the static-template code block under `### ARC:product-context Content` — already completed in T01.1.
- Naming `/arc-sync` as the sole writer and explicitly noting `/arc-wave` and `/arc-ship` no longer write the block (writer-attribution sentence) — T01.3.
- Updating the `### CLAUDE.md Creation` rule warning text and the `## Cross-References` list — T01.3.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | `T01.2-01-test.txt` | PASS — `grep -n '^## Update Behavior$' bootstrap-protocol.md` returns zero matches (exit 1); legacy section heading removed |
| 2 | test | `T01.2-02-test.txt` | PASS — Field/Source/Fallback table header row absent (count 0); all 5 data-source row phrases (`docs/skill/temper/management-report.md`, `First sentence of docs/VISION.md`, `persona names from docs/CUSTOMER.md`, `spec-ready counts from docs/BACKLOG.md`) absent |
| 3 | file | `T01.2-03-file.txt` | PASS — new `## Migration and Idempotency` H2 present at line 89, positioned between `## Coexistence Rules` (line 64) and `## Cross-References` (line 95); narrative contains both required elements (wholesale-replacement code path with explicit mention of legacy live and already-migrated static; idempotency with byte-identical guarantee) |
| 4 | file | `T01.2-04-file.txt` | PASS — Unit 1's headline grep `grep -nE 'Backlog:\|Current Wave:\|Phase:\|Primary Personas:\|Vision:' bootstrap-protocol.md` now returns zero matches across the entire file (was previously matching inside the table rows that T01.2 removed) |

## Spec Acceptance Criteria Coverage (Unit 1, partial)

This subtask satisfies the migration-and-idempotency portion of Unit 1's functional requirements (feature scenarios "Migration overwrites prior content with the static template via the same code path as routine writes", "Idempotency is documented as a guarantee", and "The legacy 'Update Behavior' table is removed"):

- [x] The "Update Behavior" table mapping Vision/Phase/Wave/Personas/Backlog fields to data sources is removed.
- [x] No table maps managed-section fields to artifact data sources.
- [x] The section contains no references to dynamic data sources for the static block.
- [x] The document states that any content found between BEGIN/END markers is replaced wholesale.
- [x] The document states the same code path applies to legacy live blocks and already-migrated static blocks.
- [x] The document does not describe a separate "detect legacy format" branch.
- [x] The document states that running the protocol twice produces identical bytes between marker positions.
- [x] The new section's position relative to `## Coexistence Rules` and `## Cross-References` is preserved.

Remaining Unit 1 requirements (writer attribution naming `/arc-sync` only and explicitly noting `/arc-wave` and `/arc-ship` no longer write, CLAUDE.md Creation rule update, cross-references update) are scoped to T01.3.

## Sanitization

Proof artifacts contain only markdown content from the public spec, the bootstrap-protocol reference, and the new narrative authored under T01.2. No secrets, tokens, credentials, connection strings, or sensitive data.
