# T01.1 Proof Summary

**Task:** Replace ARC:product-context content schema with static template
**Spec:** `docs/specs/17-spec-claude-md-static-references/17-spec-claude-md-static-references.md` (Unit 1)
**File modified:** `skills/arc-wave/references/bootstrap-protocol.md`
**Status:** COMPLETED
**Timestamp:** 2026-04-27T18:24Z

## Scope

Replace the live-content code block under `### ARC:product-context Content` (containing `**Vision:**`, `**Phase:**`, `**Current Wave:**`, `**Primary Personas:**`, `**Backlog:**` lines) with the static template (intro sentence + four em-dash hint bullets linking to `docs/BACKLOG.md`, `docs/ROADMAP.md`, `docs/VISION.md`, `docs/CUSTOMER.md`). Delete the explanatory `> **Note:**` paragraph that described how counts were derived. Preserve `### Markers` and `## Insertion Algorithm` sections unchanged.

## Out of Scope (Other T01 Subtasks)

- Removing the `## Update Behavior` table (T01.2)
- Adding migration + idempotency narrative (T01.2)
- Attributing `/arc-sync` as sole writer (T01.3)
- Updating CLAUDE.md Creation rule and cross-refs (T01.3)

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | file | `T01.1-01-file.txt` | PASS — verifies the static template's six structural elements (H2, intro, 4 link bullets) are present at lines 26-33 |
| 2 | test | `T01.1-02-test.txt` | PASS — `grep -nE 'Backlog:\|Current Wave:\|Phase:\|Primary Personas:\|Vision:' bootstrap-protocol.md` returns zero matches inside the static-block template (exit code 1) |
| 3 | file | `T01.1-03-file.txt` | PASS — the explanatory `> **Note:**` paragraph about derived counts has been deleted (zero grep matches for "are derived") |
| 4 | file | `T01.1-04-file.txt` | PASS — surrounding `### Markers`, `## Insertion Algorithm`, `### 1. Check for Existing Section`, `### 2. Insertion Priority`, `### 3. Validate Insertion` sections are preserved at original positions |

## Spec Acceptance Criteria Coverage (Unit 1, partial)

This subtask satisfies the content-template portion of Unit 1's functional requirements:

- [x] Static block has `## Product Context` H2 heading
- [x] Static block has short intro sentence stating live status lives in linked artifacts, not this section
- [x] Static block has bulleted list — one bullet each for `docs/BACKLOG.md`, `docs/ROADMAP.md`, `docs/VISION.md`, `docs/CUSTOMER.md`
- [x] Each bullet ends with em-dash + one-line usage hint
- [x] Block continues to use `<!--# BEGIN ARC:product-context -->` / `<!--# END ARC:product-context -->` markers
- [x] No backlog counts, current wave name, persona list, phase, or vision summary in template
- [x] Explanatory Note paragraph about derived counts is removed
- [x] `### Markers` and `## Insertion Algorithm` sections preserved unchanged

Remaining Unit 1 requirements (Update Behavior table removal, migration narrative, idempotency narrative, writer attribution, CLAUDE.md Creation rule update, cross-references update) are scoped to T01.2 and T01.3.

## Sanitization

Proof artifacts contain only markdown content from the public spec and the bootstrap-protocol reference. No secrets, tokens, credentials, or sensitive data.
