# T04.2 Proof Summary

**Task:** T04.2 — Verify idempotency and byte-identical preservation outside ARC markers
**Status:** COMPLETED — both correctness properties hold; no escalation back to T04.1 required.
**Target file:** `/home/ron.linux/arc/CLAUDE.md` (post-T04.1 state)
**Migration commit under verification:** 99fb385 (T04.1)

This sub-task does not modify CLAUDE.md. It only validates T04.1's output against
two correctness properties drawn from `skills/arc-wave/references/bootstrap-protocol.md`.

## Proof Results

| # | File | Type | Property | Result |
|---|------|------|----------|--------|
| 1 | T04.2-01-cli.txt | cli | Idempotency: re-applying the static-block algorithm yields zero diff | PASS |
| 2 | T04.2-02-cli.txt | cli | Byte-identical preservation outside ARC markers (above + below) | PASS |
| 3 | T04.2-03-cli.txt | cli | `git diff 99fb385^ 99fb385 -- CLAUDE.md` confined strictly to between BEGIN/END markers | PASS |

## Verification Details

### Property 1 — Idempotency (T04.2-01-cli.txt)

A Python script extracted the bytes between the BEGIN/END ARC:product-context
markers from the post-T04.1 CLAUDE.md (832 bytes), compared them to the canonical
template literal copied verbatim from `skills/arc-wave/references/bootstrap-protocol.md`
(also 832 bytes), and confirmed byte equality. The script then performed a
destructive re-write replacing the inner region with the same template bytes
and ran `git diff --exit-code -- CLAUDE.md`:

- Inner-region byte equality: True
- Full-file byte equality after re-write: True
- `git diff` exit code: 0 (no changes)
- `git diff` stdout: empty

A second application of the protocol is a no-op diff, confirming the post-migration
file is a fixed point of the algorithm.

### Property 2 — Byte-identical outside markers (T04.2-02-cli.txt)

The pre-migration content was extracted via `git show 99fb385^:CLAUDE.md`. Both
the pre- and post-migration content were partitioned at the BEGIN and END
markers, and the regions outside the markers were compared:

- Above-marker bytes: 1728 in both versions, byte-identical
- Below-marker bytes: 516 in both versions, byte-identical
- BEGIN marker byte position: 1728 in both (markers preserved at the same offset)

### Property 3 — Diff scope check (T04.2-03-cli.txt)

`git diff 99fb385^ 99fb385 -- CLAUDE.md` produces a single hunk (`@@ -32,10 +32,12 @@`).
All four `-` lines and all six `+` lines fall strictly between the BEGIN marker
(unchanged context, line 32) and the END marker (unchanged context, line 41 in
HEAD~1 / line 43 in HEAD). Markers themselves are unchanged. No other hunks
exist, so the file's preamble (lines 1-31) and the SKILLZ:installed-skills
section (lines 43-49 in HEAD) are untouched.

## Conclusion

Both correctness properties hold:

1. **Idempotency** — A second run of the static-block algorithm produces zero diff.
2. **Byte-identical outside markers** — All edits are confined to the region between
   the BEGIN and END ARC:product-context markers.

T04.1's output is correct. T04.2 does not require escalation back to T04.1. T04
(the parent task) is now ready to be marked completed by the lead.
