# T03.1 Proof Summary

**Task:** T03.1 - Document Step 11 reminder-note behavior in SKILL.md
**Spec:** `docs/specs/14-spec-wave-time-estimate-opt-in/14-spec-wave-time-estimate-opt-in.md`
**Demoable Unit:** Unit 3 - Post-creation reminder note in the wave summary
**Date:** 2026-04-17
**Worker:** worker-6

## Scope

Updated Step 11 of `skills/arc-wave/SKILL.md` (the "Offer Next Steps" section)
to document the post-creation reminder-note behavior introduced by Unit 3 of
spec 14. The documentation is purely textual (no behavior change to any other
skill step) and lives directly above the existing Step 11 `AskUserQuestion`
code block so implementers and future maintainers have a single, unmistakable
reference for:

- The exact reminder-note wording.
- The four emission rules that govern when and how the note is printed.

Only `skills/arc-wave/SKILL.md` was modified.

## Changes

- Added a **Post-creation reminder note (informational only)** labelled block
  directly below the Step 11 heading and directly above the existing
  `AskUserQuestion` code block.
- The block contains:
  1. A one-sentence lead that states when the note fires (when `target` is
     unset) and where it is emitted (alongside the Step 11 `AskUserQuestion`
     call).
  2. The exact reminder text rendered as a markdown blockquote, preserved
     verbatim from the task description:
     `Tip: no time estimate was captured. Add one by editing docs/ROADMAP.md or rerunning /arc-wave.`
  3. A numbered list of four emission rules, each phrased verbatim per the
     task description:
     - Rule 1: emit as plain assistant text alongside the AskUserQuestion
       call — never as an option inside it.
     - Rule 2: emit only when `target` is unset.
     - Rule 3: do not emit when `target` has a value.
     - Rule 4: never prompt for or collect an estimate during the
       post-creation summary (informational only).
  4. A final sentence naming the note text as the single source of truth to
     prevent future paraphrases, translations, or punctuation drift.
- The existing Step 11 `AskUserQuestion` code block (four options,
  `multiSelect: false`) is preserved intact — no option, label, description,
  or question field was modified or inserted.
- The existing **Handle selection:** list immediately below the code block is
  preserved intact.

## Proof Artifacts

| # | Type | Description | File | Status |
|---|------|-------------|------|--------|
| 1 | cli  | Verbatim reminder-note string is present exactly once in Step 11 of SKILL.md | `T03.1-01-cli.txt` | PASS |
| 2 | cli  | All four emission rules are documented verbatim in a numbered list above the AskUserQuestion code block | `T03.1-02-cli.txt` | PASS |
| 3 | file | Step 11 AskUserQuestion code block and Handle-selection list are preserved intact; reminder block is clearly distinct from wave report content | `T03.1-03-file.txt` | PASS |

## Requirement Coverage

From Unit 3 functional requirements in
`docs/specs/14-spec-wave-time-estimate-opt-in/14-spec-wave-time-estimate-opt-in.md`,
T03.1 satisfies the documentation requirement:

- `The system shall document the note's exact text in skills/arc-wave/SKILL.md
  Step 11 so behavior is reproducible.` — satisfied by the blockquote at
  SKILL.md line 363 containing the verbatim note text.

T03.1 additionally documents (so downstream implementers have an unambiguous
reference) the four emission rules that back these Unit 3 behavioral
requirements — the behavioral enforcement itself is covered by sibling task
T03 (pending):

- `The system shall emit a one-line plain-text note in the post-wave summary
  when target is unset, in the form: "Tip: no time estimate was captured. ..."`
- `The system shall emit the note in the text response printed alongside
  Step 11's AskUserQuestion — not as an extra option inside the question.`
- `The system shall not emit the note when target has a value.`
- `The system shall not prompt, ask, or collect the estimate during the
  post-creation summary — the reminder is informational only.`

## Gherkin Coverage

From
`docs/specs/14-spec-wave-time-estimate-opt-in/post-creation-reminder-note-in-wave-summary.feature`:

- **Scenario: Reminder wording is documented in SKILL.md** — directly
  satisfied: Step 11 of SKILL.md now contains the exact reminder-note text
  verbatim AND documents the rule that the note is emitted only when
  `target` is unset (emission rule 2).
- **Scenario: Summary shows reminder when estimate is skipped** — emission
  rule 1 documents that the note is printed as plain assistant text
  alongside the Step 11 `AskUserQuestion` call; emission rule 2 documents
  the unset-only trigger.
- **Scenario: Summary omits reminder when estimate is present** — emission
  rule 3 documents the suppression path.
- **Scenario: Reminder is not rendered as an AskUserQuestion option** —
  emission rule 1 explicitly forbids embedding the note as an option,
  label, description, or question inside the `AskUserQuestion` call.
- **Scenario: Reminder does not prompt for input** — emission rule 4
  explicitly forbids any prompt, follow-up question, or
  estimate-collection path at Step 11.

Behavioral verification of all scenarios is covered by sibling task T03.2
(pending) which verifies the Gherkin scenarios against the SKILL.md
documentation this task produced.

## Design Considerations Honored

From the spec's Design Considerations section:

- The reminder note is documented as plain text in the assistant's response,
  not as a markdown artifact written to disk. The SKILL.md guidance
  explicitly frames the note as "plain assistant text" and "informational
  only".
- The note's placement is specified as above or below the `AskUserQuestion`
  call, clearly distinct from the wave report content (which belongs to
  Step 10, not Step 11).
- No UI color, icon, or emoji is introduced — the documentation uses only
  standard markdown headers, a blockquote, and a numbered list.

## Scope Boundaries

Files modified by T03.1:

- `skills/arc-wave/SKILL.md` — Step 11 only (lines 357-394). No other Step
  in SKILL.md was touched.

Files explicitly NOT modified (covered by other tasks or out of scope for
T03.1):

- `skills/arc-wave/SKILL.md` Step 4 (covered by T01.1 — completed)
- `skills/arc-wave/SKILL.md` Step 6 (covered by T02.1 — completed)
- `skills/arc-wave/SKILL.md` Step 10 (covered by T02.2 — pending)
- `skills/arc-wave/references/wave-report-template.md` (covered by T02.3 — completed)
- `templates/ROADMAP.tmpl.md` (covered by T02.4 — completed)
- `docs/specs/14-spec-wave-time-estimate-opt-in/*.feature` (read-only reference — covered by T03.2 verification)
- `docs/ROADMAP.md` (back-compat: no existing-wave migration)
- `docs/skill/arc/waves/*.md` (archived waves not modified)

## Sanitization

No credentials, API keys, tokens, passwords, or connection strings appear
in any proof file or in the modified SKILL.md section. All proof content
is derived from public Arc skill documentation. A scan for common secret
patterns (`sk-`, `pk_`, `api_key`, `apiKey`, `Bearer `, `password=`,
`secret=`, `PRIVATE KEY`) returns no matches in the proof directory.

## Status

PASS - All proof artifacts pass. Task ready to commit.
