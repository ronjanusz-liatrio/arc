# T02.3 Proof Summary

**Task:** T02.3 - Document TBD placeholder in wave-report-template.md
**Spec:** docs/specs/14-spec-wave-time-estimate-opt-in/14-spec-wave-time-estimate-opt-in.md
**File modified:** skills/arc-wave/references/wave-report-template.md

## Goal

Update `skills/arc-wave/references/wave-report-template.md` so the Target header line documents both its valid rendered forms:

1. Populated form: `**Target:** {timeframe or milestone}`
2. Placeholder form (used when no time estimate was captured): `**Target:** TBD (use /arc-wave to add)`

This documentation gives implementers and future maintainers a single source of truth for the placeholder convention introduced by Unit 2 of spec 14.

## Changes

- Added an HTML comment in the Template block, directly below the `**Target:**` line, pointing readers at the placeholder rule without breaking the renderable template example.
- Added a new `### Target` subsection at the top of the Field Descriptions section that:
  - Shows the populated form verbatim in a fenced markdown block.
  - Shows the placeholder form `**Target:** TBD (use /arc-wave to add)` verbatim in a fenced markdown block.
  - Cross-references Step 6 of `skills/arc-wave/SKILL.md` (the ROADMAP wave entry) so implementers of T02.1 and the renderer for Step 10 stay aligned.
  - Clarifies the ROADMAP summary table uses the shorter `TBD` while the wave report header uses the full placeholder.
  - Notes that existing wave reports with populated Target values are never rewritten (preserves back-compat).

## Proof Artifacts

| # | Type | File | Status | Evidence |
|---|------|------|--------|----------|
| 1 | cli  | T02.3-01-cli.txt  | PASS | `grep -n "TBD (use /arc-wave to add)"` returns 2 matches in the template file (lines 14 and 81). |
| 2 | cli  | T02.3-02-cli.txt  | PASS | `grep -n "Target:\*\* {timeframe or milestone}"` returns 2 matches (lines 12 and 76) confirming the populated form is still present as the default template and is also echoed as a documented example. |
| 3 | file | T02.3-03-file.txt | PASS | The file's Field Descriptions section now contains a `### Target` subsection with both forms plus cross-reference text. |

## Gherkin Coverage

Scenario from `render-tbd-placeholder-when-target-is-unset.feature`:

> Scenario: Wave report template documents both populated and placeholder forms
>   Given the repository contains `skills/arc-wave/references/wave-report-template.md`
>   When a reader opens the template file
>   Then the template shows an example `**Target:**` line with a populated value
>   And the template shows an example `**Target:**` line with `TBD (use /arc-wave to add)` for the unset case
>   And both example forms are rendered in the order and style matching a real wave report

All three Then/And clauses are satisfied by the updated template file:

- Populated form present at line 12 (Template block) and line 76 (Field Descriptions).
- Placeholder form present at line 14 (HTML comment in Template block) and line 81 (Field Descriptions code block).
- Both forms rendered in markdown style matching a real wave report header.

## Status

PASS - All proof artifacts pass. Task ready to commit.
