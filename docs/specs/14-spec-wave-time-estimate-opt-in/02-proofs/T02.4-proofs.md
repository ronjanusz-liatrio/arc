# T02.4 Proof Summary

**Task:** T02.4 - Mark Target Timeframe optional in ROADMAP.tmpl.md
**Spec:** `docs/specs/14-spec-wave-time-estimate-opt-in/14-spec-wave-time-estimate-opt-in.md`
**Demoable Unit:** Unit 2 - Render `TBD (use /arc-wave to add)` placeholder when Target is unset
**Date:** 2026-04-17
**Worker:** worker-3

## Scope

Updated `templates/ROADMAP.tmpl.md` so that the Target Timeframe field is
documented as optional starting at the Vertical Slice phase and carrying through
the Foundation / MVP / Growth / Maturity phases (which inherit Foundation via
the existing "All [prior phase] sections, maintained and current" language).
Spike and Proof of Concept remain "Not Required" and were not modified.

The placeholder guidance is documented verbatim in the template:

- Detailed wave entry renders `TBD (use /arc-wave to add)` when `target` is unset.
- Wave Summary Table cell renders the shorter `TBD` when `target` is unset.

## Proof Artifacts

| # | Type | Description | File | Status |
|---|------|-------------|------|--------|
| 1 | cli  | Documented placeholder string `TBD (use /arc-wave to add)` appears in template | `T02.4-01-cli.txt` | PASS |
| 2 | cli  | Target Timeframe marked "optional" in Vertical Slice and Foundation phase sections | `T02.4-02-cli.txt` | PASS |
| 3 | file | Spike and Proof of Concept phase sections remain "Not Required" (unchanged) | `T02.4-03-file.txt` | PASS |

## Requirement Coverage

From the spec's Unit 2 functional requirements:

- `The system shall update templates/ROADMAP.tmpl.md to note that Target
  Timeframe is optional at Vertical Slice and later phases, with the documented
  placeholder form.` - satisfied by the Vertical Slice content-guidance bullet
  (line 55) and the Foundation per-wave bullet renamed to "Target Timeframe
  (optional)" (line 78).

Related Gherkin scenario (in
`docs/specs/14-spec-wave-time-estimate-opt-in/render-tbd-placeholder-when-target-is-unset.feature`):

- Scenario "ROADMAP template notes Target is optional at Vertical Slice and
  later phases" - satisfied: the Vertical Slice section indicates Target
  Timeframe is optional AND the documented `TBD (use /arc-wave to add)`
  placeholder is shown as the unset rendering.

## Inheritance Note

MVP, Growth, and Maturity phases explicitly inherit Foundation's Required
sections via:

- MVP: "All Foundation sections, maintained and current."
- Growth: "All MVP sections, maintained and current."
- Maturity: "All Growth sections, maintained and current."

That means the Foundation-level "Target Timeframe (optional)" bullet is
transitively inherited by MVP / Growth / Maturity without per-phase
duplication. This matches the template's existing inheritance convention.

## Sanitization

No credentials, API keys, tokens, passwords, or connection strings appear in
any proof file or in the modified template. Proof files contain only public
template content.

## Out of Scope (Not Modified)

- `skills/arc-wave/SKILL.md` (covered by T01.1, T02.1, T02.2, T03.1)
- `skills/arc-wave/references/wave-report-template.md` (covered by T02.3)
- `docs/ROADMAP.md` (no existing-wave Target values are rewritten per
  the spec's back-compat requirement).
