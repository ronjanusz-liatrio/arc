# T02.2 Proof Summary

**Task:** T02.2 - Update Step 10 wave report header renderer for TBD placeholder
**Spec:** `docs/specs/14-spec-wave-time-estimate-opt-in/14-spec-wave-time-estimate-opt-in.md`
**Demoable Unit:** Unit 2 - Render `TBD (use /arc-wave to add)` placeholder when Target is unset
**Date:** 2026-04-17
**Worker:** worker-7

## Scope

Updated Step 10 of `skills/arc-wave/SKILL.md` (the wave-report header
renderer) so the rendering rule for the `**Target:**` header line in the
wave report is unambiguous:

- **Populated form** — render `**Target:** {timeframe}` verbatim using the
  captured value (e.g., `**Target:** 2 weeks`).
- **Unset form** — render the exact literal line
  `**Target:** TBD (use /arc-wave to add)` when `target` is unset (empty
  string or missing — the default after Step 4).
- **Consistency** — the placeholder form matches the one used in the Step 6
  ROADMAP wave entry (owned by T02.1), so the reader sees the same reminder
  hint in both places.
- **Cross-reference** — guidance points readers to
  `skills/arc-wave/references/wave-report-template.md` (Field Descriptions
  → Target, owned by T02.3) for the documented populated and placeholder
  forms.
- **Back-compat** — pre-existing wave report files are neither rewritten
  nor normalized; the placeholder rule applies only to the wave report
  being generated for the newly created wave.

Only `skills/arc-wave/SKILL.md` was modified. The existing wave-report
template markdown block inside Step 10 (lines 311-355) was preserved
verbatim — only additional rendering-rule prose was appended below the
closing code-fence, mirroring the pattern established by T02.1 for Step 6.

## Proof Artifacts

| # | Type | Description | File | Status |
|---|------|-------------|------|--------|
| 1 | cli  | Literal placeholder string `TBD (use /arc-wave to add)` present in Step 10 | `T02.2-01-cli.txt` | PASS |
| 2 | cli  | Step 10 block documents populated form, unset form, and cross-references | `T02.2-02-cli.txt` | PASS |
| 3 | file | Scope boundaries respected — only `skills/arc-wave/SKILL.md` modified | `T02.2-03-file.txt` | PASS |

## Requirement Coverage

From the spec's Unit 2 functional requirements, T02.2 satisfies:

- `The system shall render the **Target:** header line in the wave report
  (Step 10) as **Target:** TBD (use /arc-wave to add) when target is
  unset.` — satisfied by the new renderer guidance at SKILL.md line 360.
- The populated-form rendering (`**Target:** {timeframe}`) is preserved
  by the existing template block at SKILL.md line 316 and made explicit
  by the new "When `target` is set" guidance at SKILL.md line 359.

Related Gherkin scenarios (in
`docs/specs/14-spec-wave-time-estimate-opt-in/render-tbd-placeholder-when-target-is-unset.feature`):

- "Wave report header shows TBD placeholder when estimate is skipped" —
  satisfied: Step 10 guidance mandates the literal line for the unset case
  and explicitly states the placeholder matches the ROADMAP wave entry
  form, so the scenario's "placeholder string matches the one used in the
  ROADMAP wave entry" assertion is upheld.
- "A populated Target value renders without the TBD placeholder" —
  satisfied: the "When `target` is set" branch renders the captured value
  verbatim and does not invoke the placeholder form (shared with T02.1
  coverage for Step 6).

Out-of-scope requirements (covered by sibling tasks):

- Step 6 ROADMAP wave-entry rendering — T02.1 (completed).
- `skills/arc-wave/references/wave-report-template.md` — T02.3 (completed).
- `templates/ROADMAP.tmpl.md` — T02.4 (completed).
- Step 4 prompt removal — T01.1 (completed).
- Step 11 reminder note — T03.1 (completed).

## Design Considerations Honored

From the spec's Design Considerations section:

- The full placeholder `TBD (use /arc-wave to add)` is used verbatim in the
  wave-report header (Step 10), matching the form used in the ROADMAP
  detailed wave entry (Step 6).
- No emoji, icon, or color is introduced — only plain markdown prose.
- The wave report file path (`docs/skill/arc/wave-report.md`) and
  surrounding structure remain unchanged.

## Sanitization

No credentials, API keys, tokens, passwords, or connection strings appear
in any proof file or in the modified SKILL.md section. All proof content
is derived from public Arc skill documentation.

## Out of Scope (Not Modified)

- `skills/arc-wave/references/wave-report-template.md` (covered by T02.3)
- `templates/ROADMAP.tmpl.md` (covered by T02.4)
- `skills/arc-wave/SKILL.md` Step 4 (covered by T01.1)
- `skills/arc-wave/SKILL.md` Step 6 (covered by T02.1)
- `skills/arc-wave/SKILL.md` Step 11 (covered by T03.1)
- `docs/ROADMAP.md` (back-compat: no existing-wave migration)
- `docs/skill/arc/waves/*.md` (archived waves not modified)
- `docs/skill/arc/wave-report.md` (generated output; not a skill source file)
