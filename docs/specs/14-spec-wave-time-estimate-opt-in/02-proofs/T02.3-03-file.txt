Type: file
Target: skills/arc-wave/references/wave-report-template.md
Expected: The file contains a "### Target" Field Descriptions subsection that documents both valid forms of the Target header line: the populated form "**Target:** {timeframe or milestone}" and the placeholder form "**Target:** TBD (use /arc-wave to add)".
Timestamp: 2026-04-17T00:00:00Z

Excerpt (lines 70-90):
## Field Descriptions

### Target
The `**Target:**` header line has two valid rendered forms:

- **Populated form** - when a time estimate was captured, render verbatim:
  ```markdown
  **Target:** {timeframe or milestone}
  ```
  For example: `**Target:** 2 weeks` or `**Target:** Q3 milestone`.
- **Placeholder form** - when no time estimate was captured at wave creation, render the literal line:
  ```markdown
  **Target:** TBD (use /arc-wave to add)
  ```

The placeholder string is used verbatim (not `TBD`, not `TBD (...)` with a different hint). It matches the form used in the ROADMAP wave entry generated by Step 6 of `skills/arc-wave/SKILL.md`. The ROADMAP summary table uses the shorter `TBD` value in its `Target` column, but the wave report header always uses the full placeholder form so the reader sees the same reminder hint in both places.

Existing wave reports with a populated Target value are never rewritten or normalized - the placeholder only applies to newly created waves that skip the estimate.

Result: The Field Descriptions section of wave-report-template.md now includes a dedicated "### Target" subsection that explicitly documents both the populated form and the TBD placeholder form, along with cross-references to Step 6 of SKILL.md and the shorter ROADMAP summary-table rendering. The Template block above also contains an HTML comment adjacent to the Target line that points readers at the placeholder rule, keeping the template block renderable (the comment does not break markdown syntax).

Status: PASS
