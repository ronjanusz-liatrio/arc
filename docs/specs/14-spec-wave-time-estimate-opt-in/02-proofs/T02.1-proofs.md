# T02.1 Proof Summary

**Task:** T02.1 - Update Step 6 ROADMAP wave-entry renderer for TBD placeholder
**Spec:** `docs/specs/14-spec-wave-time-estimate-opt-in/14-spec-wave-time-estimate-opt-in.md`
**Demoable Unit:** Unit 2 - Render `TBD (use /arc-wave to add)` placeholder when Target is unset
**Date:** 2026-04-17
**Worker:** worker-4

## Scope

Updated Step 6 of `skills/arc-wave/SKILL.md` (the ROADMAP wave-entry renderer)
so the rendering rule for the `**Target:**` line is unambiguous:

- **Detailed wave entry** — render `**Target:** {timeframe}` when `target`
  is set; render the literal `**Target:** TBD (use /arc-wave to add)` when
  `target` is unset.
- **ROADMAP summary table `Target` column** — render the captured
  `{timeframe}` when set; render the short literal `TBD` (no parenthetical
  hint) when unset, so table cells stay concise per the spec's Design
  Considerations.
- **Back-compat** — prior wave entries in `docs/ROADMAP.md` are neither
  rewritten nor reformatted; the placeholder applies only to newly appended
  waves.

Only `skills/arc-wave/SKILL.md` was modified. The existing template markdown
block inside Step 6 (lines 211-228) was preserved verbatim — only additional
rendering-rule prose was appended below it.

## Proof Artifacts

| # | Type | Description | File | Status |
|---|------|-------------|------|--------|
| 1 | cli  | Literal placeholder string `TBD (use /arc-wave to add)` present in Step 6 | `T02.1-01-cli.txt` | PASS |
| 2 | cli  | Step 6 block documents populated form, unset form, and short-`TBD` table rule | `T02.1-02-cli.txt` | PASS |
| 3 | file | Scope boundaries respected — only `skills/arc-wave/SKILL.md` modified | `T02.1-03-file.txt` | PASS |

## Requirement Coverage

From the spec's Unit 2 functional requirements, T02.1 satisfies:

- `The system shall render the **Target:** line in the ROADMAP wave entry
  (Step 6 of skills/arc-wave/SKILL.md) as **Target:** TBD (use /arc-wave to add)
  when target is unset.` — satisfied by the new renderer guidance at
  SKILL.md lines 230-233.
- `The system shall render the ROADMAP summary table Target column cell as
  TBD when target is unset (table cells stay short; the parenthetical hint
  only appears in the detailed wave entry and wave report header).` —
  satisfied by the new summary-table renderer guidance at SKILL.md
  lines 237-240.
- `The system shall preserve any existing Target: value in waves already
  present in docs/ROADMAP.md ... — no rewriting or migration is performed.`
  — satisfied by the explicit back-compat note at SKILL.md line 242.

Related Gherkin scenarios (in
`docs/specs/14-spec-wave-time-estimate-opt-in/render-tbd-placeholder-when-target-is-unset.feature`):

- "ROADMAP wave entry shows TBD placeholder when estimate is skipped" —
  satisfied: Step 6 guidance mandates the literal line for the unset case.
- "ROADMAP summary table Target column shows short TBD" — satisfied: the
  summary-table renderer rule explicitly forbids the parenthetical hint in
  the table cell and requires `TBD` short form.
- "Existing Target values in prior ROADMAP waves are preserved" — satisfied:
  the back-compat rule scopes the placeholder to the newly appended wave only.
- "A populated Target value renders without the TBD placeholder" — satisfied:
  the "When `target` is set" branch renders the captured value verbatim and
  does not invoke the placeholder form.

Out-of-scope requirements (covered by sibling tasks):

- Step 10 wave report header rendering — T02.2 (pending).
- `skills/arc-wave/references/wave-report-template.md` — T02.3 (completed).
- `templates/ROADMAP.tmpl.md` — T02.4 (completed).
- Step 4 prompt removal — T01.1 (completed).
- Step 11 reminder note — T03.1 (pending).

## Design Considerations Honored

From the spec's Design Considerations section:

- The full placeholder `TBD (use /arc-wave to add)` appears in the detailed
  wave entry.
- The short form `TBD` is used in the ROADMAP summary table so cells stay
  concise. The SKILL.md guidance explicitly calls this out and forbids the
  parenthetical hint from appearing inside the table cell.
- No emoji, icon, or color is introduced — only plain markdown prose.

## Sanitization

No credentials, API keys, tokens, passwords, or connection strings appear
in any proof file or in the modified SKILL.md section. All proof content
is derived from public Arc skill documentation.

## Out of Scope (Not Modified)

- `skills/arc-wave/references/wave-report-template.md` (covered by T02.3)
- `templates/ROADMAP.tmpl.md` (covered by T02.4)
- `skills/arc-wave/SKILL.md` Step 4 (covered by T01.1)
- `skills/arc-wave/SKILL.md` Step 10 (covered by T02.2)
- `skills/arc-wave/SKILL.md` Step 11 (covered by T03.1)
- `docs/ROADMAP.md` (back-compat: no existing-wave migration)
- `docs/skill/arc/waves/*.md` (archived waves not modified)
