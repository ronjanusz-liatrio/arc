# Clarifying Questions — Round 1

## Q1: Which Python tool caches should be added?

**Answer:** All — `.venv/`, `__pycache__/`, `.mypy_cache/`, `.pytest_cache/`, `.ruff_cache/`, `.tox/`, `*.egg-info/`

## Q2: Which other language/framework build dirs should be added?

**Answer:** `target/`, `.gradle/`, `.next/`, `.nuxt/`, `coverage/`
