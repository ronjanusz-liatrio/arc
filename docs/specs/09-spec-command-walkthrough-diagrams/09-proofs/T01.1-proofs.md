# T01.1 Proof Summary

## Task
Create scripts/lint-mermaid.sh with fence extraction and mmdc validation

## Results

| # | Type | Description | Status |
|---|------|-------------|--------|
| 1 | cli | Baseline lint run: 9 files, 11 fences, all valid (exit 0) | PASS |
| 2 | cli | Broken fence detection: reports file + fence index (exit 1) | PASS |
| 3 | file | shellcheck clean + executable bit set in git index (100755) | PASS |
| 4 | cli | Temp directory cleanup confirmed via trap | PASS |

## Proof Artifacts
- `T01.1-01-cli.txt` -- Baseline lint run output
- `T01.1-02-cli.txt` -- Broken fence failure output
- `T01.1-03-file.txt` -- shellcheck + executable verification
- `T01.1-04-cli.txt` -- Temp file cleanup verification

## Notes
- `npx --yes @mermaid-js/mermaid-cli` is invoked directly (not via `mmdc` subcommand) as the package bin is `mmdc` itself
- `--parseOnly` is not available in mermaid-cli 11.12.0; uses full render to svg with `-q` (quiet) flag
- Script is compatible with macOS bash 3.2 (no mapfile, no GNU-specific flags)
