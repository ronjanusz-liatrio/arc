# T01.2 Proof Summary

Task: T01.2 — Validate lint script (shellcheck, chmod, baseline sweep)

## Results

| # | Type | Description | Status |
|---|------|-------------|--------|
| 1 | cli | shellcheck scripts/lint-mermaid.sh — zero warnings | PASS |
| 2 | cli | File permissions — executable (-rwxr-xr-x) | PASS |
| 3 | cli | Baseline sweep — 9 files, 11 fences, all valid (exit 0) | PASS |
| 4 | cli | Broken fence detection — exit 1, names file and fence index | PASS |

## Details

### Proof 1: shellcheck
Ran `shellcheck scripts/lint-mermaid.sh` — produced no warnings or errors.
The script uses `set -euo pipefail`, proper quoting, and safe array handling.

### Proof 2: chmod
File already had correct permissions (`-rwxr-xr-x`) as set by T01.1.

### Proof 3: Baseline sweep
Ran `bash scripts/lint-mermaid.sh` against the full repo (pre-Unit 2 tree).
Output: `9 files, 11 fences, all valid` with exit code 0.
The script correctly found 9 markdown files with mermaid fences (excluding docs/specs)
and validated all 11 fences via mermaid-cli.

### Proof 4: Broken fence detection
Created `_scratch-broken.md` with an intentionally malformed mermaid graph
(`A[Start --> B[End]` — missing closing bracket). The script output
`FAIL: _scratch-broken.md fence #1` and exited with code 1. The scratch file
was then removed.

## Conclusion

All four validation criteria pass. The lint script is production-ready:
shellcheck-clean, executable, validates valid fences (exit 0), and correctly
detects and reports broken fences (exit 1).
